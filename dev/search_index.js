var documenterSearchIndex = {"docs":
[{"category":"section","location":"metrics/#Scoring-and-metrics","page":"Scoring and metrics","text":"","title":"Scoring and metrics"},{"category":"section","location":"metrics/#Scoring","page":"Scoring and metrics","text":"Every scalar metric reports two scores, and a function is flagged when either fires:\n\nAbsolute: the value against a fixed band (cyclomatic warn >10 / high >20, nesting >3, parameters >4). A fixed target a codebase can improve toward.\nRelative: the value's percentile against the corpus. Catches functions worse than the codebase's own norm, the signal that matters in review.\n\nAbsolute alone misses outliers in a uniformly-weak codebase; relative alone calls a uniformly-weak codebase fine. Reporting both avoids each trap.","title":"Scoring"},{"category":"section","location":"metrics/#Metrics","page":"Scoring and metrics","text":"Scalar (per function): cyclomatic complexity, cognitive complexity (the same branch points weighted by the nesting they sit under, so a deeply-nested function scores worse than a flat one of the same path count), length, maximum nesting depth, parameter count, boolean complexity (the most &&/|| operators joined into one expression).\n\nFlag (presence is the finding): swallowed errors (empty catch clauses), stub markers (TODO/FIXME/XXX/HACK comments), empty function bodies, a return inside a finally clause (which discards a pending error or return value), identical operands (x == x, a && a), a conditional whose branches are all identical (if c then X else X), unused parameters, unused locals, and broad catches (a bare except:, except BaseException, Java catch (Throwable), C++ catch (...), Ruby rescue Exception, PHP catch (Throwable), the handlers that swallow interrupts and exits; the merely-wide except Exception tier is left alone). An optional rule flags code after an unconditional return, break, or throw.\n\nUnused parameters and locals read the lexical bindings: a parameter or local binding whose name nothing in its function references is dead weight. The use-test is by name over the whole unit, the conservative reading, so a same-named reference in a nested closure counts as a use. A leading underscore opts a name out, the cross-language deliberate-unused convention. A bodyless declaration keeps its parameters (they are its signature), an empty or stub body is already the empty_body finding, and a top-level binding belongs to :unreferenced, so none of those double-report. A language whose parameters carry no names (bash) or whose scopes query captures no locals (php) reports nothing for that half.\n\nThree more coupling and binding readings are optional rules: local_count (distinct local names bound in a function), shadowed_variable (a fresh local binding hiding an enclosing one), and fan_out (distinct callables a function invokes). See Custom rules.\n\nEach metric is a rule. The set above is the default; a caller can add their own or opt into rules that are off by default.\n\nRelational (computed across the corpus, not per function): duplicates and opt-in reimplementation candidates (below), naturalness, within-file cohesion, and cross-file placement. Naturalness scores each function's token sequence against a per-language trigram model of the rest of the corpus, in bits per token. The corpus model is interpolated with a per-file cache model (after Tu et al., \"On the Localness of Software\"), so a function is read against its own file's idiom, not just the corpus's, which sharpens genuine outliers and quiets file-consistent patterns. A surprising, unidiomatic function scores high, and surprise correlates with bugs. Reported as :unnatural with both scores, the absolute cross-entropy band and the corpus percentile. A language with too few tokens to model is skipped.\n\nCohesion asks whether a file's functions group by usage (below). Placement asks whether a unit sits in the right file, scattering whether a file's units belong to one module, and reachability whether a private definition is dead, reported as :unreferenced (below).","title":"Metrics"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"Dendro exports nothing. Its public API is marked with the public keyword. Import what you call, or qualify with Dendro..","title":"API reference"},{"category":"section","location":"api/#Analysis","page":"API reference","text":"","title":"Analysis"},{"category":"section","location":"api/#Gating","page":"API reference","text":"","title":"Gating"},{"category":"section","location":"api/#Findings","page":"API reference","text":"","title":"Findings"},{"category":"section","location":"api/#Rules","page":"API reference","text":"","title":"Rules"},{"category":"section","location":"api/#Pattern-rules","page":"API reference","text":"","title":"Pattern rules"},{"category":"section","location":"api/#Configuration","page":"API reference","text":"","title":"Configuration"},{"category":"function","location":"api/#Dendro.analyze","page":"API reference","text":"analyze(path; base=nothing, cut=nothing, min_size=nothing, threshold=nothing, radius_factor=nothing, language=nothing, rules=nothing, ignore=String[], config=nothing) -> Findings\nanalyze(paths::AbstractVector; ...) -> Findings\n\nAnalyze the file or folder at path. Every function gets scalar and flag metrics; functions duplicated across the corpus are reported as :duplicate findings, and functions that are close but not identical as :near_duplicate. A baseline is built from the corpus, the folder's files or the single file, so relative scoring works against the input's own distribution with no setup. With base, only functions changed against that git ref are reported, scored against the full-corpus baseline.\n\nPassing several paths folds their files into one corpus, so a package's src and ext are scanned together (analyze([\"src\", \"ext\"])) without dragging in the rest of the tree. The baseline, duplicate detection, and naturalness span the roots, so a function copied from one into another is caught. With base, all roots resolve to the one git toplevel and the repo-wide diff scopes them.\n\nthreshold is the LCS-similarity cutoff for a near-miss, radius_factor scales the candidate-search radius to a function's size.\n\nThe :reimplementation pass, vocabulary-overlap pairs the structural passes miss, is off by default and enabled only through the config, [rules] reimplementation = true in a .dendro.toml or a config built here; the rules kwarg carries Rules and cannot express it. Its overlap cutoff is the config's [reimplementation] threshold.\n\nThresholds come from a Config: the bands, the percentile cut, and which rules are active. By default analyze discovers one, merging a user-global config and the repo .dendro.toml over the built-in defaults. Pass config to supply one directly and skip discovery. An explicit cut or rules overrides the config, so a caller keeps the final say.\n\ncut is the percentile cutoff a corpus-relative metric flags above; it defaults to the config's, 0.95 absent a file. rules is the active rule set; absent, it is the config's resolution of BUILTIN_RULES and the enabled OPTIONAL_RULES. Pass your own to lint for a project's structural conventions: analyze(path; rules = [BUILTIN_RULES; my_rule]).\n\nignore is a list of gitignore-style patterns, matched against each path relative to a scanned folder. Matching files are dropped before parsing, so vendored or generated source is neither flagged nor counted in the baseline: analyze(path; ignore = [\"vendor/\", \"*.generated.jl\"]). A leading ! re-includes, a trailing / matches directories only. As in gitignore, a file under an excluded directory cannot be re-included. Patterns apply to folder scans, not a single named file.\n\n\n\n\n\n","title":"Dendro.analyze"},{"category":"function","location":"api/#Dendro.active","page":"API reference","text":"active(findings) -> Findings\n\nThe findings not suppressed by an inline directive. Use this for gating.\n\n\n\n\n\n","title":"Dendro.active"},{"category":"function","location":"api/#Dendro.github_annotations","page":"API reference","text":"github_annotations(io, findings)\ngithub_annotations(findings)\n\nWrite findings as GitHub Actions workflow commands, one ::error/::warning line per finding, anchored at its first location. GitHub records each as a pull-request check annotation; it renders inline on the diff when the anchored line is part of the change, otherwise in the run's Checks tab. Pair this with analyze's base to scope findings to the functions a change touched. Suppressed findings are omitted. High-band findings map to ::error, the rest to ::warning.\n\n\n\n\n\n","title":"Dendro.github_annotations"},{"category":"function","location":"api/#Dendro.mermaid","page":"API reference","text":"mermaid(io, paths; graph=:coupling, granularity=:file, focus=:auto, context=1, ...)\nmermaid(paths; ...) -> nothing\n\nRender one of Dendro's graphs over paths as a mermaid flowchart, written to io. graph selects the diagram: :coupling the corpus reference graph behind :misplaced and :scattered, :reachability the dead-code graph behind :unreferenced, :clones the duplicate clusters. granularity is :file (units collapsed to their file) or :unit (one node per function). Active findings overlay onto the diagram: misplaced and scattered nodes are classed, the misplaced suggested home drawn as a dashed edge, dead definitions and public roots classed.\n\nfocus trims the diagram to what the findings touch. :findings keeps only flagged nodes and the context hops of graph neighbours around them, drawn greyed, so a unit-level graph of a real corpus stays small enough to read and to render. :all keeps every node. :auto (the default) resolves to :findings at :unit granularity, where the full graph is a hairball, and :all at :file, where it is already legible. context is the neighbour radius: 0 keeps only the flagged nodes, 1 their immediate neighbours. :clones is already finding-only, so focus does not change it.\n\nUnlike github_annotations, which renders Findings, this takes the corpus and rebuilds the structure it draws, since a graph is not recoverable from findings. Redirect io to a .mmd file to save the diagram, as the CI workflow does for annotations. The keyword options match analyze's clone and ignore tuning.\n\nprofiles is the language registry the corpus resolves file extensions through. It defaults to the languages Dendro ships; pass Dendro.resolve_profiles(config) to draw a graph over a language a .dendro.toml registers, which analyze does from its own config.\n\n\n\n\n\n","title":"Dendro.mermaid"},{"category":"function","location":"api/#Dendro.errors","page":"API reference","text":"errors(paths; since=nothing, config=nothing, rules=nothing, ignore=String[], language=nothing) -> Findings\n\nThe error-severity findings over paths: the deterministic floor, every finding at the :high absolute band (high-band scalars and all flags), with inline dendro-ignore directives applied first so a suppressed finding lifts the gate.\n\nThis is the gate companion to analyze. analyze ranks by corpus percentile for triage and so is never empty; errors reads only the fixed bands, so it is satisfiable and stable, suitable for a CI gate. Assert isempty(errors(path)) in a test and every package's existing Pkg.test() gates on Dendro for free.\n\nLike analyze, errors honors a Config: it discovers one from the repo .dendro.toml unless a config is passed, so a project that retunes a band or toggles a rule sees the change in the gate, not only in analyze. The same config scores the working tree and the since base, so a retuned band never reads as a regression on its own.\n\nWith since, a git ref, the result is the ratchet: the floor at the working tree minus the floor at that ref. A finding the change introduced is reported; one that predates the ref, even on a line the change touched, is not. This answers \"did this change introduce a violation\", and supports incremental adoption on a codebase that is not yet clean. A since that names no commit throws: a broken ref is CI misconfiguration, never a silent fall-back to the floor.\n\nsince is distinct from analyze's base. base is spatial, restricting findings to changed lines for annotations; since is a finding-set difference, the gate.\n\nconfig, rules, ignore, and language pass through to analyze; rules absent, the active set is the config's resolution of BUILTIN_RULES and the enabled OPTIONAL_RULES.\n\n\n\n\n\n","title":"Dendro.errors"},{"category":"type","location":"api/#Dendro.Finding","page":"API reference","text":"Finding\n\nOne reported issue over one or more locations. Per-file metrics fire at a single location; relational metrics like :duplicate span several.\n\nmetric: which metric fired (:cyclomatic, :empty_catch, :duplicate, ...).\nlocations: every site the finding covers, at least one.\nvalue: the scalar reading, the member count for :duplicate, or nothing.\nabsolute: :ok/:warn/:high band, always :high for flags.\npercentile: corpus rank in [0, 1], or nothing when no corpus sample ranks against it.\nkind: :scalar or :flag.\nsuppressed: whether an inline directive accepted this finding.\n\n\n\n\n\n","title":"Dendro.Finding"},{"category":"type","location":"api/#Dendro.Findings","page":"API reference","text":"Findings <: AbstractVector{Finding}\n\nThe result of analyze: the findings it produced, printed as a report. Behaves as an AbstractVector{Finding}, so it iterates, filters, and indexes like any vector of Findings.\n\n\n\n\n\n","title":"Dendro.Findings"},{"category":"type","location":"api/#Dendro.Location","page":"API reference","text":"Location\n\nA code site: its file path, 1-based line, enclosing unit name (\"\" when no name node is found), and an optional label, what this site means to the finding carrying it.\n\nA label is what lets a finding name an edit rather than only score one. :scattered counts how many communities a file's units are pulled into; the label on each location says which file it is pulled toward, which is the move. :split_audience and :hub label each representative with the files consuming its audience, which is the split. Without them a reader has to rebuild the corpus graph to recover what the pass already computed.\n\nA label is evidence, not identity: the gate's fkey reads file and unit alone, so labelling a site never moves a ratchet key and never re-reports a finding. Where the label is the identity, :dependency_cycle's choice of which edge to cut, it goes in unit instead and does enter the key.\n\n\n\n\n\n","title":"Dendro.Location"},{"category":"type","location":"api/#Dendro.Rule","page":"API reference","text":"Rule\n\nOne lint rule. name is the metric a finding reports under and the name an inline dendro-ignore directive accepts. kind is :scalar or :flag. A scalar rule carries its (warn, high) band; a flag rule carries nothing. fn measures one unit or the whole index:\n\nscalar fn(unit, index) -> Int, scored against the band and the corpus percentile.\nflag fn(index) -> Vector{TreeSitter.Node}, one finding per returned node.\n\nseverity is the absolute band a flag rule's findings carry, and is meaningful only for a flag: a scalar reads its band instead. Every built-in flag is :high, which is what puts it in the errors floor. A user-authored pattern rule defaults to :warn, since a rule firing across a corpus would otherwise make the gate unsatisfiable. The two kind-specific fields mirror each other: band is scalar-only, severity flag-only.\n\n\n\n\n\n","title":"Dendro.Rule"},{"category":"constant","location":"api/#Dendro.BUILTIN_RULES","page":"API reference","text":"BUILTIN_RULES :: Vector{Rule}\n\nThe default rule set, in report order. Scalar bands are fixed (warn, high) targets, so a uniformly-weak codebase has a standard to improve toward rather than only its own median. Drawn from common complexity guidance. Pass rules to analyze to extend or replace them.\n\n\n\n\n\n","title":"Dendro.BUILTIN_RULES"},{"category":"constant","location":"api/#Dendro.OPTIONAL_RULES","page":"API reference","text":"OPTIONAL_RULES :: Vector{Rule}\n\nRules a caller can opt into but that are off by default: return_count needs per-project band tuning, trivial_wrapper has a higher false-positive rate, unreachable_after_jump flags code after an unconditional jump, npath grows multiplicatively so its band wants per-project tuning, local_count likewise, shadowed_variable reads name collisions some idioms make routine (a method local matching a class attribute), and fan_out cannot separate a smell from a legitimate orchestrator by any fixed band (idiomatic corpora run p99 from 9 to 26 distinct callees). Use them with analyze(path; rules = [BUILTIN_RULES; OPTIONAL_RULES]).\n\n\n\n\n\n","title":"Dendro.OPTIONAL_RULES"},{"category":"type","location":"api/#Dendro.PatternSpec","page":"API reference","text":"PatternSpec\n\nOne user-authored lint rule, as declared by a [patterns.<name>] table in a .dendro.toml. Where a Rule carries the function that measures, a spec carries what a project said it wants; pattern_rule turns one into the other once the query behind it has compiled.\n\nname is the metric a finding reports under and the name an inline dendro-ignore accepts. message is what the rule says when it fires. severity is :warn or :high, deciding whether the rule reaches the errors floor. kind is :flag or :scalar; a scalar carries its (warn, high) band and a flag carries nothing.\n\nThe declaration is language-independent. One <lang>.patterns.scm per grammar realises it, so a rule says one thing across every language a project scans.\n\n\n\n\n\n","title":"Dendro.PatternSpec"},{"category":"function","location":"api/#Dendro.check_patterns","page":"API reference","text":"check_patterns(paths; config=nothing) -> Vector{PatternTestFailure}\n\nRun every declared pattern rule against the fixtures in its pattern directories and report where a fixture and the rule disagree.\n\nFixtures live in a tests/ folder beside the queries, one file per language, and mark the lines a rule must match with a dendro-expect: <rule> comment. A marked line the rule did not match is a miss; an unmarked line it did match is a false positive. Both fail.\n\nWhat a fixture pins is what the query matched, for either kind of rule. A scalar rule's band and percentile are Dendro's own scoring and are tested elsewhere, so a scalar fixture marks the lines its occurrences sit on exactly as a flag fixture does.\n\nA rule with no fixture is not a failure. Requiring one would be friction on writing a two-line house rule, and the zero-match report already catches the rule that never fires at all.\n\n\n\n\n\n","title":"Dendro.check_patterns"},{"category":"type","location":"api/#Dendro.PatternTestFailure","page":"API reference","text":"PatternTestFailure\n\nOne disagreement between a fixture and what a rule did: file and line locate it, rule names the rule, and kind is :missed (a marked line the rule did not report) or :unexpected (an unmarked line it did).\n\n\n\n\n\n","title":"Dendro.PatternTestFailure"},{"category":"type","location":"api/#Dendro.Config","page":"API reference","text":"Config\n\nResolved tuning thresholds for one analysis, built by discover_config from the built-in defaults and a .dendro.toml. cut is the percentile cutoff; bands overrides scalar rule (warn, high) tuples by metric name; one field per relational metric overrides that metric's band; rules toggles a rule on or off by name, and the reimplementation, incoherent_package and divisible_package corpus passes with it; min_size, threshold, and radius_factor are the clone-detection thresholds; reimpl_threshold is the reimplementation overlap cutoff; languages carries the languages the config registers beyond the ones Dendro ships, each a LanguageProfile naming where to load its grammar and queries from; and patterns carries the user-authored lint rules the config declares, each a PatternSpec, sorted by name so a report reads in a stable order, and patterns_dir overrides where their queries are read from (empty for the default, .dendro/patterns beside the config). Immutable: pass one to analyze with config = to skip file discovery.\n\n\n\n\n\n","title":"Dendro.Config"},{"category":"section","location":"languages/#Languages-and-limitations","page":"Languages and limitations","text":"","title":"Languages and limitations"},{"category":"section","location":"languages/#Languages","page":"Languages and limitations","text":"bash, c, cpp, go, java, javascript, julia, php, python, ruby, rust, typescript.\n\nJSON and HTML are out of scope: with no functions or control flow, these metrics do not apply.","title":"Languages"},{"category":"section","location":"languages/#Registering-a-language","page":"Languages and limitations","text":"Adding a language is a tree-sitter query, a profile entry, and an extension entry. A project supplies all three from its .dendro.toml, so a language Dendro does not ship needs no fork:\n\n[languages.zig]\nextensions = [\"zig\"]\ngrammar = \"/path/to/tree-sitter-zig\"\nqueries = \"/path/to/my-queries\"\n\nqueries is the only required key, naming a directory that holds <name>.scm and optionally <name>.scopes.scm and <name>.imports.scm. grammar defaults to the language name and resolves to tree_sitter_<name>_jll; point it at a directory instead to load a grammar from a local tree-sitter repository, which needs a tree-sitter.json and a built shared library beside it. That is the only route for a grammar with no JLL in the General registry. extensions are the file types the language claims, with or without a leading dot; omit them to keep whatever the built-in table already maps for that name.\n\nA registered language of the same name as a built-in replaces it, which is how a project retunes a shipped query without a fork:\n\n[languages.python]\nqueries = \"my-queries\"\n\nWrite the scopes query rather than leaving it out. Without one the language loses cohesion scoring and every binding-based flag (unused_parameter, unused_local, shadowed_variable, local_count), which is a large share of what Dendro reports. The passes skip a language that ships no scopes query rather than scoring it wrongly.\n\nCross-file resolution needs more than a query: a linkage entry is Julia code in the package, so a registered language reaches the per-file metrics, the flags, and clone detection, but none of the passes built on cross-file references: :misplaced, :scattered, :unreferenced, :split_audience, :back_edge, :dependency_cycle, :hub, :incoherent_package, or :divisible_package.\n\nexamples/languages/ in the repository carries a worked Zig query, both files, along with the reasoning behind the choices a query has to make.","title":"Registering a language"},{"category":"section","location":"languages/#What-cross-file-resolution-reaches-per-language","page":"Languages and limitations","text":"Every rule past a single file rests on matching a reference to the definition it names along declared linkage. How far that reaches differs by language, and where it reaches nothing the rules go quiet rather than reporting a problem. A quiet report is not the same as a clean one, so it is worth knowing which case you are in.\n\nJulia, Python, C, Go and Rust resolve normally: a reference names a top-level function, so both the unit graph and the file graph fill in and every rule applies.\n\nJava and PHP resolve types, not methods. A method is reached through a receiver whose type the resolver never works out, so what one Java or PHP file sees of another is the class name. The file graph is unaffected and well populated, 1220 edges across Guava's collect package and 304 across Laravel's Database, so :back_edge, :dependency_cycle, :hub and :split_audience all work, and so does :divisible_package. The unit graph carries no cross-file edge at all, because a class definition is not a function unit, which leaves :misplaced, :scattered and :incoherent_package silent on a Java or PHP corpus. That is the type-and-dispatch line holding, not a gap to close: following a method call to its definition is exactly the resolution Dendro does not do.\n\nJavaScript resolves ES modules only. The linkage query reads import ... from and export. A corpus written in CommonJS, require() and module.exports, resolves nothing across the boundary, so every cross-file rule is silent on it. Express and Lodash are both in this position.\n\nRuby resolves require_relative only. A plain require names a file through a load path the source does not carry, and a method inside a module or class body is not spliced into the requiring file's namespace in any case, so a conventionally-written Ruby library resolves nothing. Sinatra is in this position.\n\nTypeScript resolves its ESM output convention, where a specifier names the emitted ./a.js and the source on disk is a.ts.","title":"What cross-file resolution reaches per language"},{"category":"section","location":"languages/#Limitations","page":"Languages and limitations","text":"Ruby swallowed-rescue is not flagged. Its handler body is inline rather than a block, so it does not fit the detection model.\nSwitch default adds one to complexity in C, C++, and Java (default shares the case node) but not in Go, JavaScript, TypeScript, PHP, or Ruby (default has its own node).\nGo empty-body detection is weak: a Go function body always wraps a statement list, so empty bodies do not register.\nMetrics are syntactic. Dendro resolves names lexically, within a file and across declared include/import/export edges, but never types or dispatch. Concerns that need type or dispatch resolution (overload resolution, real call graphs, dead code across files) are out of scope.\nCross-file placement sees only the linkage a language ships a query for, and only the include/import edges present in the scanned corpus. A name matching several visible definitions is resolved by name, not dispatch, so its weight is split across them. Dynamic imports and re-exports are not followed.","title":"Limitations"},{"category":"section","location":"patterns/#Pattern-rules","page":"Pattern rules","text":"A Rule measures the concept vocabulary a language's <lang>.scm query produces: decision points, nesting, catch clauses, parameters. That vocabulary is closed on purpose, since it is what keeps metric code free of grammar node types. It also means a rule cannot name a shape Dendro has no concept for.\n\nA pattern rule can. It is a second query family, <lang>.patterns.scm, where a capture names a rule instead of a concept. The set of names is open and the queries are written against concrete grammar node types deliberately, because being language-specific is the point.","title":"Pattern rules"},{"category":"section","location":"patterns/#Declaring-a-rule","page":"Pattern rules","text":"A rule is declared once, language-independently, in .dendro.toml:\n\n[patterns.empty_catch_binding]\nmessage  = \"`catch` with no exception binding discards the error\"\nseverity = \"high\"\n\n[patterns.magic_number]\nmessage = \"unnamed numeric literals in one function\"\nkind    = \"scalar\"\nband    = [5, 10]\n\nkey \nmessage required; what the rule says when it fires\nseverity \"warn\" (default) or \"high\", for a flag rule\nkind \"flag\" (default) or \"scalar\"\nband required by a scalar, rejected on a flag; must start at 1 or higher\n\nseverity defaults to warn because errors gates on the :high band. A rule that fires across a corpus would otherwise make the gate unsatisfiable the day it is written. Promoting one to high is a line of config and the project's call.\n\nA scalar's band starts at 1 or higher so that a unit with no matches scores :ok. A band starting at zero would report every function in the corpus for containing nothing.\n\nMetadata lives here and never beside the queries. A rule name is language-independent and a query is not, so a message kept next to a query would be either per language and free to drift, or in a second config file with a second cascade to merge. A pattern rule also toggles off through [rules] by the same name a built-in does:\n\n[rules]\nmagic_number = false","title":"Declaring a rule"},{"category":"section","location":"patterns/#Writing-the-query","page":"Pattern rules","text":"Queries live in .dendro/patterns/<lang>.patterns.scm beside the config, and in ~/.config/dendro/patterns/ for rules shared across repositories. Both locations are read and compose; where both define the same rule for the same language, the repo's wins for that pair.\n\n; A capture names a rule.\n(catch_clause) @empty_catch_binding\n\n; A `.not` capture subtracts from the rule of the same name, by node identity. Write\n; the pattern, then the more specific pattern that should be excluded.\n(catch_clause (identifier)) @empty_catch_binding.not\n\n; A `_` prefix marks a capture a predicate needed, never a rule of its own.\n((call_expression (identifier) @_f) @eval_call (#eq? @_f \"eval\"))\n\nThree conventions carried by the capture name:\n\n@rule is a match to report.\n@rule.not subtracts from @rule by node identity. Because the two patterns land on the same anchor node, negation is predictable and does not depend on where inside a match the exclusion sat. This is how a rule says \"without\": a catch binding nothing, a switch with no default, a function with no return type annotation.\n@_anything is a helper. A predicate has to reference a capture, so #eq? @_f \"eval\" forces capturing the identifier it tests; without the prefix that capture would become a rule firing on every node the predicate examined.\n\nSeveral patterns may share one capture name and accumulate, which is how one rule carries more than one spelling:\n\n((call_expression function: (member_expression object: (identifier) @_o))\n @debug_output (#eq? @_o \"console\"))\n(debugger_statement) @debug_output\n\nEvery other capture must have a [patterns.<name>] table. A capture naming no declared rule is an error at load, which is what catches a typo and a helper that lost its underscore.","title":"Writing the query"},{"category":"section","location":"patterns/#Predicates","page":"Pattern rules","text":"The predicates available are the ones TreeSitter.jl implements: #eq?, #not-eq?, #any-of?, #match?, #not-match?, #has-ancestor?, #is?, #is-not?, #any-eq?, #any-not-eq?, #any-match?, #any-not-match?, and #set!. Anything else is rejected when the rule loads.\n\n#not-any-of? and #not-has-ancestor? do not exist; a .not capture covers both. Prefer #eq? and #any-of? to #match? where either works, since a match's predicates are re-evaluated once per capture and a regex rule reruns it each time.","title":"Predicates"},{"category":"section","location":"patterns/#Fragments","page":"Pattern rules","text":"A shape repeated across rules factors out. A ; fragment: comment defines a piece of query text and a reference splices it:\n\n; fragment: loops = [(while_statement) (for_statement)]\n\n@loops @any_loop\n(function_definition (block @loops)) @loop_in_function\n\nExpansion is one level: a reference inside a fragment is left alone rather than expanded recursively. A fragment cannot share a name with a declared rule, since a capture cannot be both a shape to splice and a rule to report.","title":"Fragments"},{"category":"section","location":"patterns/#Flag-rules-and-scalar-rules","page":"Pattern rules","text":"A flag rule reports one finding per matched node, carrying its declared severity as the absolute band. A scalar rule counts its matches per unit and is scored against its band and the corpus percentile, exactly as cyclomatic is, including retuning through [bands]:\n\n[bands]\nmagic_number = [3, 6]\n\nA scalar counts through the same fold every built-in scalar uses, so it stops at nested callables: a closure's matches belong to the closure, not to the function around it.\n\nBecause a pattern rule resolves to an ordinary Rule, everything else follows with no special handling. Findings carry dendro-ignore suppression by rule name, scope to a diff under analyze's base, reach errors when their band is :high, and participate in the ratchet under errors(paths; since).","title":"Flag rules and scalar rules"},{"category":"section","location":"patterns/#When-a-rule-is-wrong,-it-says-so","page":"Pattern rules","text":"A rule that reports nothing reads as clean code, which is the failure this design works hardest to prevent. Every way of getting a rule wrong is loud.\n\nTree-sitter validates a query when it compiles, so a node type the grammar does not have, an unknown field, a capture reference no pattern binds, and a syntax error all fail at load, naming the file and the line:\n\ndendro: node type error at line 3 of .dendro/patterns/julia.patterns.scm\n\nA predicate TreeSitter.jl does not implement compiles cleanly and then rejects every match, so it is rejected at load instead, with the alternatives named. A capture naming no declared rule is an error, and so is a fragment sharing a rule's name. An error inside an expanded fragment names the fragment, where it was defined, and where it was used, rather than an offset into text nobody wrote.\n\nThe one case none of that reaches is a well-formed query naming a shape that simply never occurs. A rule that matched nothing anywhere in the corpus is reported after the scan:\n\nwarning: pattern rule(s) matched nothing: no_such_shape","title":"When a rule is wrong, it says so"},{"category":"section","location":"patterns/#Testing-a-rule","page":"Pattern rules","text":"Those catch a rule that is malformed, not one that fires on the wrong thing. Fixtures pin both directions. They live in a tests/ folder beside the queries, one file per language, and mark the lines a rule must match:\n\n# .dendro/patterns/tests/julia.jl\ntry r() catch    # dendro-expect: empty_catch_binding\nend\ntry r() catch e  # unmarked, so the rule must not fire here\nend\n\n$ dendro --check-patterns src/\n\nA marked line the rule missed is a failure, and so is an unmarked line it matched. Checking for false positives is the point: a check that only asked whether a rule matched something would pass a rule that matched everything.\n\nA fixture records what the query matched, for either kind of rule. A scalar rule's band and percentile are Dendro's own scoring and are tested elsewhere, so a scalar fixture marks the lines its occurrences sit on exactly as a flag fixture does.\n\nA rule with no fixture is not a failure. Requiring one would be friction on writing a two-line house rule, and the zero-match report already covers the rule that never fires.\n\ncheck_patterns is the same check from Julia, returning a PatternTestFailure per disagreement, so a project can assert on it in its own suite.","title":"Testing a rule"},{"category":"section","location":"patterns/#What-a-pattern-rule-cannot-do","page":"Pattern rules","text":"A query reads a node's shape, its text, and its ancestry. It cannot see bindings and it cannot resolve a type, which is the same line every other part of Dendro holds.\n\nA rule needing either stays a Julia Rule passed to analyze's rules, described under Custom rules. That path is reachable from Julia but never from a config file: loading Julia named by a repo's .dendro.toml would execute it during a scan.\n\nMeasured against the default rule sets of Clippy, Ruff, and ESLint, roughly half of a sampled 150 rules are expressible as pattern rules. The remainder needs types or trait resolution (about a sixth), is formatting and node ordering (about a quarter), needs binding resolution (a twentieth), or is something Dendro already reports. The fit is best for Python, at around 70%, and weakest for Rust, at 44%, where linting resolves traits.","title":"What a pattern rule cannot do"},{"category":"section","location":"cohesion/#Cohesion-and-placement","page":"Cohesion and placement","text":"","title":"Cohesion and placement"},{"category":"section","location":"cohesion/#Within-file-cohesion","page":"Cohesion and placement","text":"analyze reports files whose functions split into independent concerns, as :low_cohesion. It builds a graph of a file's functions and links two when they reference a common file-local name, a helper, type, or constant defined in the same file. A file that breaks into several disconnected components holds that many concerns living together, the LCOM4 reading of low cohesion. The finding's value is the component count, and its locations are one representative function per component:\n\nsrc/util.jl:1  parse_date  low_cohesion 3 (warn)\n    also at src/util.jl:40  render_html\n    also at src/util.jl:88  open_socket\n\nTo link on the name's binding rather than the bare string, Dendro resolves each reference to the definition it refers to within the file (tree-sitter locals-style scopes, src/queries/<lang>.scopes.scm). This drops the noise a string graph carries: a local x in one function and a same-named x in another are different bindings, and an imported or builtin name resolves to nothing. A binding referenced by most of the file's functions is a cross-cutting utility, not a shared concern, and links nothing. Reported with both scores, the absolute band on the component count and the corpus percentile.\n\nThe resolution is lexical, never dispatch: an edge means two functions reference the same file-local name, not that they call the same method. The signal stays syntactic and within one file. Cohesion runs for every supported language; each ships a scopes query.\n\nThe lexical line has a cost in class-based code. An edge is call linkage, two functions naming the same file-local definition, not shared-field cohesion: with no symbol or field resolution, methods that touch the same instance field through different names form no edge. The reading is weakest for a file that is one class, where field-sharing is the main cohesion and Dendro sees only method-to-method calls. Java is the extreme, since every file is one class.","title":"Within-file cohesion"},{"category":"section","location":"cohesion/#Cross-file-placement","page":"Cohesion and placement","text":"Reported as :misplaced: a unit that couples more to another file than to its own. The within-file binding resolver leaves a reference unbound when its definition lives in another file. Placement resolves those references corpus-wide. A per-language linkage query (src/queries/<lang>.imports.scm) tags how files see each other's names across four models: a splice joins included files into one namespace (Julia include, C #include, Ruby require_relative); an import brings named definitions of a resolved module in (Python, JavaScript, TypeScript, Rust, PHP); a directory shares a package's names across its files (Go); a package adds, on top of imports, the same-directory types a language resolves without an import (Java). A reference that leaves its file resolves to the definition it names in a file its linkage exposes, and the result is a corpus-wide graph of which unit references which.\n\nA splice carries namespaces in some languages. A definition inside a Julia module that an included file declares does not join the includer's namespace: nothing reaches it by bare name, only by qualifying it (Mod.f). Such a definition resolves under its qualified name, matched on the namespace enclosing it directly, since the outer modules a reference walks through can be declared in a file the definition's own module path never sees.\n\nA definition at file scope answers to both forms. Its file was spliced into whatever module the include sits inside, a namespace the file itself never declares, so the corpus walks the splice chain to find the module path each file lands in. A file-scope name is then visible bare and qualified. Reading a qualified reference this way also stops a field read (row.total) resolving as a bare total: the two are the same syntax, and a value's field matches no namespace.\n\nThe score is the envy percent, the share of a unit's whole coupling, own-file and cross-file, that lands in the single other file it leans toward most. A unit devoted to one other file scores near 100; a coordinator that reaches into several files spreads its mass and stays low. The finding's first location is the unit, its second the suggested home. Two scores, like cohesion: the absolute band and the corpus percentile, fired when either trips. The deciding gate is the graph's communities (neighbourhoods, by modularity optimisation): a unit is a candidate only when its community is anchored in a file other than its own, the module the references say it belongs to.\n\nResolution is name-based and gated by declared visibility, never typed. A reference matching several visible definitions splits its weight across them rather than picking one by dispatch. A definition many units reach for is discounted as infrastructure, so a shared helper does not pull every caller toward its file, the corpus analog of the cohesion ubiquity cut. A language with no linkage query contributes no cross-file edges.\n\nReported as :scattered: a file whose units belong to several different modules, the cross-file companion to within-file :low_cohesion. The corpus graph holds only cross-file edges, so its communities alone would split every layered file. Folding each file's within-file binding edges, the same edges cohesion links on, into the graph first lets a cohesive file's units settle into one community, so only a file whose units are each drawn toward a different other file scatters. The score is the count of distinct communities the file's units occupy whose plurality anchor is another file: a file that stays home scores zero. A bag of unrelated functions is low-cohesion but not scattered, each its own self-anchored community; what scatters is a file each of whose units belongs with a different other file. Two scores, like cohesion: the absolute band and the corpus percentile.\n\nThe finding's locations are one representative unit per elsewhere-anchored community, each labelled with the file that community is anchored in. The count is the score; the labels are the edit:\n\nsrc/resolution.jl:25  corpus_symbols  [belongs with mermaid.jl]  scattered 4 (warn; p96)\n    also at src/resolution.jl:66  unbound_references  [belongs with bindings.jl]\n    also at src/resolution.jl:166  DeclaredLinkage  [belongs with linkage.jl]\n    also at src/resolution.jl:179  visible_defs  [belongs with linkage.jl]\n\nTwo units pulled toward the same file is the useful reading there: it says where the seam between the two files is currently drawn wrongly. Recovering that without the labels means rebuilding the corpus graph by hand, which is work the pass has already done.","title":"Cross-file placement"},{"category":"section","location":"cohesion/#Layout-against-coupling","page":"Cohesion and placement","text":"Reported as :incoherent_package: a directory whose units mostly belong to communities anchored in other directories. Placement reads those communities per unit and scattering reads them per file; this reads them per directory, the level a repo declares its structure at. The score is the percentage of a directory's units whose community is anchored elsewhere, so a directory of forty units is comparable with one of four, where a count of the communities it touches would not be. The finding's locations pair one representative unit per elsewhere-anchored community with the unit anchoring that community, which is how the directories the contents belong to get named.\n\nThe pass proposes a rearrangement rather than a bounded edit, and it restates per directory much of what :scattered reports per file, so it is off by default and opts in through a .dendro.toml:\n\n[rules]\nincoherent_package = true\n\nMeasured over 37 corpora, 90% of directories score zero and half a percent reach the warn band, so a project that groups its code by what it couples to sees nothing here. A directory holding fewer than three units is not scored: over that few, the percentage says more about the directory's size than about its coupling. Reading the directory from the path rather than from a declared module keeps the rule the same across languages that have modules and languages that do not.","title":"Layout against coupling"},{"category":"section","location":"cohesion/#Directories-that-outgrew-one-folder","page":"Cohesion and placement","text":"Reported as :divisible_package: a directory holding groups of children that could become subdirectories. Placement, scattering and layout all ask the outward question, whether code belongs somewhere else. This asks the inward one. A directory whose contents all belong exactly where they are can still hold several independent groups that never became folders, and then the contents are right while the internal shape is missing.\n\nThe directory is read as its direct children over the file dependency graph, a child file as one node and a child directory as one node with everything under it contracted in. The communities of that induced subgraph are the candidate groups, and the score is the best one's internal ratio as a percentage: of the reference weight its members carry inside the directory, the share that stays inside the group. A group at 95 sends nineteen references in twenty to its own members.\n\nGroups are extracted, not partitioned. Whatever no folder claims stays at the top level and is expected to, so a directory of two cohesive subsystems beside a pile of miscellaneous files yields two folders and keeps the rest loose. The first location stands for the directory and its label says how much of it the proposal places, so the leftover reads off the finding. One location per proposed folder follows, each labelled with its size and its internal ratio:\n\nsrc/backend/constfold/pass.jl:1  [src/backend, 12 of 12 children placed]  divisible_package 100 (high)\n    also at src/backend/constfold/pass.jl:1  [6 children, 100% internal]\n    also at src/backend/emit/pass.jl:1  [6 children, 100% internal]\nsrc/parser/cursor.jl:1  [src/parser, 16 of 16 children placed]  divisible_package 95 (high)\n    also at src/parser/decl.jl:1  [5 children, 95% internal]\n    also at src/parser/diagnostic.jl:1  [5 children, 95% internal]\n    also at src/parser/cursor.jl:1  [6 children, 91% internal]\n\nBoth readings are in that report. src/parser holds sixteen loose files and divides into three folders. src/backend holds twelve subdirectories and no loose files at all, and its proposal groups those subdirectories under two new parents. Same score, same gates, same proposal, and only the things being moved differ. Contracting a child directory into one node is what makes one rule cover both: score a directory on its files alone and a fully-subdivided one has no nodes and is never asked the question.\n\nA finding proposes one level and stops. A group that itself wants dividing says so on the next scan, once it is a directory rather than a proposal, which terminates on its own because each level's children are fewer.\n\nSeveral gates decide whether the question applies before any ratio is read. A directory below twelve children is small enough to read at a glance and does not want subdirectories whatever its shape. A group below five children is a family rather than a folder, so eight groups of three stay silent even though they divide perfectly. A proposal placing less than a quarter of the directory leaves too much loose to be worth the move, and a lone folder covering more than seven tenths of it is a rename rather than a split. Where fewer than three quarters of the children carry any resolved reference the rule declines to score the directory at all: an unreferenced child is either genuinely independent or a reference the resolver missed, and nothing syntactic tells the two apart, so a score built on the connected part would measure the resolver rather than the code.\n\nTwo kinds of child are dropped before the groups are read. One that most of its siblings reach for is an artifact of cross-cutting coupling rather than a subsystem, and left in it pulls the whole directory into one group. One that names the directory it sits in (lib.rs, mod.rs, __init__.py, index.ts) cannot move into a subdirectory of it. Both stay at the top level, like everything else the proposal does not extract.\n\nThe pass proposes a rearrangement rather than a bounded edit, so it is off by default and opts in through a .dendro.toml:\n\n[rules]\ndivisible_package = true\n\nThe band comes from directories whose factoring is known because they were generated, not from any corpus. Three groups of eight files read 100 when nothing crosses between them, 75 at one cross-reference per file, 59 at two and 44 at four, so warn at 60 sits just above the level where groups are too entangled to separate without trading one tangle for another, and high at 85 above the level where they are nearly independent. Warn is also the bar a group clears to be proposed, so retuning the band under [bands] retunes what gets proposed with it. Dendro's own src reads 44 over six groups and stays quiet, which is the rule declining to call recognisable layers a modular decomposition. A directory deliberately kept flat is accepted with dendro-ignore: divisible_package in the file the finding reports it at.","title":"Directories that outgrew one folder"},{"category":"section","location":"cohesion/#Dependencies-against-the-grain","page":"Cohesion and placement","text":"Reported as :back_edge: a reference running against the direction two directories have settled on. Placement and scattering read the corpus as units coupling to units. This reads it one level up, as files depending on files, contracted by directory. Between any two directories, count the reference weight each way. When A -> B carries forty references and B -> A carries two, the code has established a direction, and each of those two runs against it: an import to drop and a definition to move.\n\nThe score is the dominance percent, 100 * major / (major + minor), and higher is worse. A pair at 60/40 is a genuinely mutual dependency, a cycle rather than a violated grain. A pair at 98/2 is an established layering with a few references running backwards. The layering is inferred from the corpus, so no declared layer order and no configuration is involved. Two scores as usual, the absolute band on the dominance percent and the percentile over the pairs that couple both ways at all, which is the meaningful comparison. A pair whose majority direction carries too few references has settled on no direction and is not scored, and a project whose files all sit in one directory yields no pairs at all.\n\nThe score is a ratio, so it says the direction is one-way, never that the way back is small. A pair with a large majority side clears a high band while still carrying dozens of references home, and each of those is its own finding. Read how many findings a pair produced, not the score, to judge how large the edit is.\n\nA pair spread over more than a few file edges is therefore reported without gating: the findings still name every edge, but none of them is error severity, whatever the dominance. There is no single edit to propose for such a pair, and one architectural observation should not become a dozen errors in a CI gate. It is the same treatment a dependency cycle too tangled to cut receives.\n\nOne finding is emitted per file edge in the minority direction rather than one per pair, since the edit is to an edge. Its locations are the import statement admitting the edge, where the language declares one, then every reference site across it. That is wider than a per-file metric's locations and it is deliberate: a change that adds a use of an already-imported name introduces a back edge without touching the import's line, and a finding located only at the import would drop out under base scoping. One consequence follows in the gate. errors(; since) keys a finding by its location set, so adding a reference to an established back edge re-reports it. That is the intended reading: the ratchet catches worsening, and another reference across a back edge is worsening.\n\nThe width rule runs the other way, which is worth knowing before you rely on the gate here. A change that adds one more back-referencing file, taking a pair just past the width limit, demotes every finding on that pair at once: violations that were error severity before the change stop being so, and the gate for that pair empties rather than fills. That is the width rule working, since none of those findings names a single edit any more, but it does mean this metric's gate contribution does not only ever grow as the coupling gets worse. The finding count does.\n\nDeliberate callbacks and plugin registration point backwards by design. Accept one with dendro-ignore: back_edge.","title":"Dependencies against the grain"},{"category":"section","location":"cohesion/#Dependency-cycles","page":"Cohesion and placement","text":"Reported as :dependency_cycle: a group of files that depend on one another in a loop, read off the same file dependency graph as :back_edge. The shape of the finding is the whole design here, because cycle membership on its own describes most of a real codebase. Measured over nine corpora it covered 319 of 4528 files, and 84% of the files in the worst of them. A rule that fires that broadly names no edit and takes the error floor with it.\n\nSo the finding is the feedback arc set, the edges whose removal would make the group acyclic. The same nine corpora put seven findings in the floor. Tarjan finds the groups; each group of two or more files then goes to the Eades-Lin-Smyth heuristic, weighted by reference count, which sends the heavy dependencies forward so that what points backwards afterwards is the light traffic, the cheaper edit. That heuristic bounds the size of the set it returns and runs in linear time. It does not return a minimum feedback arc set, and nothing in the report claims it does.\n\nThe score is the number of files in the group, against the absolute band and the corpus percentile over every cyclic group's size. The percentile fired on none of the nine calibration corpora: a corpus large enough to rank against carries enough small cycles that they tie low, so in practice the band carries this metric.\n\nThe locations are where the two kinds of finding part. Under a handful of cuts, each location is one edge to remove, at the import statement admitting it where the language declares one, labelled cut -> <target> with the target named relative to the source file's directory:\n\nsrc/session.jl:4  dependency_cycle 3 (warn; p80)\n    cut -> render.jl\n    also at src/render.jl:2  cut -> ../lib/token.jl\n\nAbove that many cuts the group has no bounded edit, so the locations become its highest-degree members and every label reads tangled: <n> cuts instead. Reporting the tangle rather than dropping it is the honest-over-silent call, and the label is what tells the two kinds apart without inferring anything from how many locations there are.\n\nThe cut label is part of the key the gate ratchet matches on, which is why the target is named relative to the source file rather than absolutely: errors(; since) scores the base revision in a temporary directory, and an absolute target would re-report every cut as new.\n\nA cycle that a language's build tolerates by design, a pair of mutually recursive modules, is accepted with dendro-ignore: dependency_cycle. Like every rule over the file graph, this one needs a linkage query for the language, and stays silent where there is none.","title":"Dependency cycles"},{"category":"section","location":"cohesion/#Hub-files","page":"Cohesion and placement","text":"Reported as :hub: a file that both depends on much of the corpus and is depended on by much of it, the Crossing anti-pattern. The reading comes from the file dependency graph, the same resolution placement uses one level up, with nothing filtered out: the score is min(fan_in, fan_out) over the count of distinct files depending on this one and the count it depends on. The conjunction is the whole signal. Fan-in alone is every utility module and fan-out alone every orchestrator, neither of which is a smell; a file with both is the one that propagates every change in either direction.\n\nThe finding's first location is the file, at its first unit. When the hub's externally-referenced definitions fall into two or more audiences, groups of definitions linked by sharing a consumer file, one representative definition per audience follows, each labelled with the files that consume it. That split is the proposed edit, and the labels are what say who each half is for:\n\nsrc/session.jl:1  hub 18 (warn; p97)\n    also at src/session.jl:12  open_session  [used by api.jl, router.jl]\n    also at src/session.jl:96  render_token  [used by view.jl, +3 more]\n\nA label naming more than a few consumers stands for the rest with a count, so a definition half the corpus reaches for does not turn the report into a list of paths.\n\nA group counts as an audience only with at least two definitions and two consumer files: a definition one file happens to use is ordinary, and a proposal built from singletons would name every definition its own audience. A hub left with fewer than two audiences carries no representatives: it is a warning with no proposal, and saying so beats proposing a split that does not hold.\n\nBecause the representatives sit in the finding's locations, a change to who consumes the file rewrites the key the gate ratchet matches on, and errors(; since) reports the hub again. That is intended: the split the earlier finding proposed no longer holds.\n\nTwo scores again, and here the corpus percentile does most of the work. Fan-in and fan-out both grow with the corpus, so the absolute band can only mark the level at which a crossing reads as central whatever the corpus, while the rank places one file against another. The ranked population is the files that cross at all, and a corpus below MIN_HUB_CORPUS_FILES is not scored: below it every file touches most of the others and the count says nothing about the architecture. A deliberate facade sits between two halves of a system by design; accept one with dendro-ignore-file: hub.","title":"Hub files"},{"category":"section","location":"cohesion/#Audience-splits","page":"Cohesion and placement","text":"Reported as :split_audience: a file whose definitions serve two or more groups of consumers that never overlap, the outward dual of :low_cohesion. Cohesion reads inward and splits a file by the bindings its own functions share; this reads outward and splits it by the files that consume its definitions. The two are independent: a file can share helpers throughout, so it reads as cohesive, while serving two audiences that never meet.\n\nFor each definition something outside the file references, Dendro collects the consumer files, links two definitions whose consumer sets meet, and takes the connected components of that graph as the file's audiences. The score is the number of audiences holding at least two definitions, so a helper with a single caller is not one. The band is [3, 5]: across ten measured corpora 85% of scored files serve a single audience and 5% serve three, so three separated interfaces is the tail and five is rare enough to gate on. Two audiences is common enough that the corpus percentile carries it. The locations are one representative definition per audience, each labelled with the files consuming that audience, which is the split the finding proposes. A file serving a single audience names no split, so it is never reported however unusual it is for the corpus, and it still counts toward the distribution the percentile reads.\n\nThe audience comes from resolved references, not from declared exports. A language with no export marker (Python, Go, C) exposes every top-level name, so an export-counting reading would collapse into file size. A file fewer than two other files consume has one audience by construction and is not scored at all.","title":"Audience splits"},{"category":"section","location":"cohesion/#Unreferenced-definitions","page":"Cohesion and placement","text":"Reported as :unreferenced: a private top-level definition no path reaches from the corpus's public surface. Dead code needs reachability, not a caller count, so a private cluster that only calls itself is still dead. The pass builds a reference graph over every top-level definition and walks forward from the roots. A definition is a root when it is declared public or referenced from top-level code, which runs unconditionally. The edges come from two sources, neither discounted: each file's within-file bindings, the same data cohesion reads, and the cross-file references placement resolves. A definition many units reach for is maximally alive, so unlike placement this graph drops no cross-cutting utility and keeps definitions that are not functions.\n\nThe public surface is per language. A name in a file's export/public list is public in Julia and JavaScript/TypeScript; a Python name is public unless it leads with an underscore; a Go name is public when it is capitalised. A per-definition visibility modifier covers the rest: a Rust item is public when it is pub, a C or C++ function is private when it is static (file-local), a method is private under a Ruby private/ protected declaration or a Java or PHP private keyword, and a Java class is private when it is package-private (not marked public). A reference is attributed to its enclosing top-level definition by byte range, so a call inside a nested helper or a lambda still keeps the enclosing function alive. A name matching several definitions keeps all of them alive, since name resolution cannot tell a type from its constructor or one method from its overload.\n\nThe reading is name-based and lexical, like the rest of placement: it matches a name to a declared definition, never resolving a type or a dispatch. Two limits follow. It is sound only over a whole module, so a private definition called from a same-module file outside the scan is falsely flagged. Runtime-only entry points (a test function, a dispatch-table callback, a string-dispatched name) carry no syntactic reference, so they are flagged unless declared public or referenced from top level; accept one with dendro-ignore: unreferenced. Java resolves a same-package reference through its :package linkage, so a package-private class with no user in its package is flagged alongside a private method. A package-private member stays public, reached same-package through a receiver the resolver does not follow. PHP checks only a private method; its classes carry no package-private privacy.","title":"Unreferenced definitions"},{"category":"section","location":"suppression/#Suppressing-findings","page":"Suppressing findings","text":"Some flagged code is fine in context. A comment directive accepts a specific finding so Dendro skips it without muting the tool or refactoring sound code. The mechanism reads comment nodes, so it works in every supported language.\n\ndendro-ignore suppresses every finding on the same line or the line directly below, so a trailing comment or a comment above a declaration both work.\ndendro-ignore: cyclomatic, parameter_count suppresses only the named metrics.\ndendro-ignore-file (or dendro-ignore-file: cyclomatic) suppresses the whole file, for generated or vendored code.\n\n# dendro-ignore: parameter_count\nfunction build(a, b, c, d, e, f)   # one keyword per field, accepted\n    ...\nend\n\nMetric names are the active rules' names plus the relational duplicate and near_duplicate: by default cyclomatic, cognitive_complexity, function_length, nesting_depth, parameter_count, boolean_complexity, empty_catch, stub_marker, empty_body, return_in_finally, identical_operands, duplicate_branches, unused_parameter, unused_local, broad_catch, duplicate, near_duplicate, reimplementation, unnatural, low_cohesion, misplaced, scattered, split_audience, unreferenced, back_edge, dependency_cycle, hub, incoherent_package, divisible_package. A custom rule's name is accepted too. An unknown name warns, so a typo does not silently disable a check. dendro-ignore-file: low_cohesion is the usual way to accept a file that is meant to be a grab-bag.\n\nSuppression marks a finding rather than dropping it. Printing a findings vector lists the active findings and a footer counting the suppressed ones, and active returns only the unsuppressed findings for gating.","title":"Suppressing findings"},{"category":"section","location":"suppression/#Ignoring-paths","page":"Suppressing findings","text":"dendro-ignore-file mutes one file from inside it. Vendored and generated trees you do not own want the opposite: exclusion from the outside, by path, without touching the source. The ignore keyword takes gitignore-style patterns, matched against each path relative to the scanned folder.\n\nanalyze(\".\"; ignore = [\"vendor/\", \"deps/**\", \"*.generated.jl\"])\n\nA leading ! re-includes, a trailing / matches directories only, * and ? stop at a separator, ** spans them. As in gitignore, a file under an excluded directory cannot be re-included.\n\nIgnored files are dropped before parsing, so they are neither flagged nor counted in the baseline. This matters even in base review mode: an unchanged vendored tree never appears in findings, but left in the corpus it would still skew the percentile every scanned file feeds. Ignoring it keeps relative scoring honest. Patterns apply to folder scans, not a single named file.","title":"Ignoring paths"},{"category":"section","location":"duplicates/#Duplicate-detection","page":"Duplicate detection","text":"analyze reports duplicates as part of a full analysis: code duplicated across the corpus, including across different files. It hashes each subtree by its structure, type not text, so fragments that differ only in variable names or literal values still match (Type-2 clones). It catches the copy-paste-then-rename that generated code produces. Detection works at two scales from the same mechanism: a whole function duplicated, or one block, an if or loop body, copied between functions that otherwise differ. A maximality filter keeps only the largest clone, so a duplicated function is reported once, not again for every block inside it. Each cluster of two or more comes back as one :duplicate finding whose locations list every member:\n\nsrc/a.jl:10  parse_header  duplicate 3 (high)\n    also at src/b.jl:42  read_header\n    also at src/c.jl:7  load_header\n\nmin_size (default 10 named nodes) gates out trivial functions, so one-line getters do not cluster. Blocks must clear twice that, since a short block of boilerplate coincides across unrelated code while a small whole function is already a meaningful unit. Pass min_size lower to widen the net, higher to focus on large clones.","title":"Duplicate detection"},{"category":"section","location":"duplicates/#Near-misses","page":"Duplicate detection","text":"Exact clustering misses the copy-paste-then-edit: two functions identical but for an added or removed statement hash differently and never meet. analyze also reports these as :near_duplicate. It compares each function's pre-order sequence of subtree hashes by longest common subsequence, after NiCad, scoring similarity as |LCS| / max(|a|, |b|), so functions that are close but not identical cluster when they clear the threshold (default 0.85). The LCS is order-aware where a multiset overlap is not: a reordering of the same statements, or a small fragment matching inside a large function, scores low and is rejected. A characteristic-vector radius query (DECKARD-style), banded by function size, finds candidate pairs without comparing every pair; the LCS similarity confirms each one. The finding's value is the cluster's weakest pairwise similarity as a percent:\n\nsrc/parser.jl:40  read_header  near_duplicate 88 (high)\n    also at src/loader.jl:12  load_header\n\nA near-miss stays syntactic and within one language, like exact detection. Pass threshold higher to demand closer matches, radius_factor to widen or narrow the candidate search.","title":"Near-misses"},{"category":"section","location":"duplicates/#Reimplementation-candidates","page":"Duplicate detection","text":"Both structural passes miss the rewrite: a helper reimplemented with different structure, a loop against straight-line code, shares no subtree shape with the original. It usually keeps the vocabulary, the same callees and the same domain words in its identifiers, and that is what the :reimplementation pass reads. Each function is fingerprinted by its callee names plus the word fragments of its identifiers, every term weighted by rarity in the scanned corpus (IDF, rebuilt on each scan). Two functions pair when the rare vocabulary they share outweighs the rest of their combined vocabulary, the finding's value that overlap as a percent:\n\nsrc/fetch.jl:12  fetch_once  reimplementation 74 (high)\n    also at src/retry.jl:30  fetch_with_retry\n\nThe evidence is vocabulary, not structure, so the finding is a proposal: two functions that talk about the same things in different shapes. The pass is off by default and opts in through a .dendro.toml:\n\n[rules]\nreimplementation = true\n\n[reimplementation]\nthreshold = 0.6   # overlap a pair must reach, the one knob\n\nFour gates keep the proposals worth reading. A pair the clone passes already report is theirs, never repeated here. A pair where one function calls the other by name is a caller and its helper, not a rewrite. Same-named functions never pair, since overloads and interface implementations share vocabulary legitimately. And a pair whose sizes differ beyond a 2:1 ratio is a fragment against a whole. Deliberately parallel families still pair, per-language implementations of one operation are vocabulary twins by design, so expect a codebase with such families to surface them; suppress with dendro-ignore: reimplementation where they are intended.\n\nThe pass stays name-based and corpus-derived, like the rest of Dendro: no types, no call graph, no pretrained model. A rewrite that also renames the domain vocabulary stays invisible to it.","title":"Reimplementation candidates"},{"category":"section","location":"duplicates/#Ranking","page":"Duplicate detection","text":"All three passes come back ordered by how much of the system a duplicate implicates. Dendro contracts the corpus by directory, groups directories into communities by how they couple, and reads each cluster's spread: within one file, within one directory, within one community of directories, or across communities. Widest spread leads. Two copies in one file are a tidy-up one reader makes in one sitting; two copies either side of a community boundary are a missing abstraction that unrelated parts of the codebase each built.\n\nThis is ordering only. A cluster's value, its band, and its locations are the same either way, so a duplicate is neither promoted into nor demoted out of the floor errors gates on. Inside one distance each pass keeps its own order: cluster size for :duplicate, similarity for :near_duplicate, overlap for :reimplementation.","title":"Ranking"},{"category":"section","location":"#Dendro.jl","page":"Home","text":"Code-maintainability metrics on tree-sitter syntax trees, a cheap automatic gate for generated code. Dendro walks the parse tree of a file, computes per-function metrics, and scores each function two ways: against fixed absolute bands and against the codebase's own distribution. Point it at a git diff and it scores only the functions a change touched.\n\nDendro never builds or runs the code. It reads syntax, so it works on a half-finished branch, on a file that does not compile, and across a dozen languages from one set of metric definitions. Every metric is a structural approximation: complexity by counting branch nodes, not by analysing control flow. Cheap and broad over precise and deep.\n\nBuilt on TreeSitter.jl. Parsers load lazily, so Dendro depends on no language grammars itself. Add the tree_sitter_<lang>_jll for the languages you analyse.","title":"Dendro.jl"},{"category":"section","location":"#Install","page":"Home","text":"import Pkg\nPkg.add(url = \"https://github.com/MichaelHatherly/Dendro.jl\")\n# plus the grammars you want to analyse, e.g.\nPkg.add([\"tree_sitter_julia_jll\", \"tree_sitter_python_jll\"])","title":"Install"},{"category":"section","location":"#Usage","page":"Home","text":"analyze is the one entrypoint. Point it at a folder or a file.\n\nDendro exports nothing; its API is marked public. Import what you call, or qualify with Dendro..\n\nusing Dendro: analyze, active\n\n# Analyse a whole project. Each function is scored against the corpus's own\n# distribution, and duplicates across files are reported. The baseline is built\n# from the corpus, so relative scoring works with no setup. The returned vector\n# prints as a report in the REPL.\nanalyze(\"src\")\n\n# Review mode: only the functions a change touched, scored against the\n# full-corpus baseline.\nanalyze(\"src\"; base = \"HEAD\")\n\n# Analyse one file. Language is inferred from the extension; the file's own\n# functions are the corpus it is scored against.\nanalyze(\"src/parser.jl\")\n\n# Scan several roots as one corpus. A package's `src` and `ext` are measured\n# together, the baseline and duplicate detection span both, and the rest of the\n# tree stays out. With `base`, the repo-wide diff scopes all roots.\nanalyze([\"src\", \"ext\"])\n\n# Capture the findings to filter or gate on.\nfindings = analyze(\"src\")\nhigh = filter(f -> f.absolute == :high, active(findings))\n\nanalyze returns Findings, a vector of Findings that prints as a report. To write that report elsewhere, show(io, MIME(\"text/plain\"), findings). A Finding carries the metric, its value, the absolute band (:ok/:warn/:high), and the corpus percentile:\n\nsrc/parser.jl:1070  predicate  cyclomatic 51 (high; p100)\nsrc/parser.jl:1070  predicate  nesting_depth 8 (high; p100)\nsrc/api.jl:289  stub_marker (high)\n\nFor pull-request review, github_annotations emits the same findings as GitHub Actions workflow commands. GitHub records each as a pull-request check annotation, high-band findings as ::error, the rest as ::warning:\n\n::error file=src/parser.jl,line=1070,title=Dendro%3A cyclomatic::predicate: cyclomatic 51 (high; p100)\n\nPair this with base to scope findings to the functions a change touched. An annotation renders inline on the diff when its anchored line falls in the change; otherwise it shows in the run's Checks tab. analyze loads each language's parser from the active environment, so the workflow adds the tree_sitter_<lang>_jll for the languages it analyses. See .github/workflows/dendro.yml for a working setup.","title":"Usage"},{"category":"section","location":"#Where-to-read-next","page":"Home","text":"Scoring and metrics explains the two-score model and what each metric measures.\nDuplicate detection covers exact clones and near-misses across the corpus.\nCohesion and placement covers cohesion, placement, scattering, and back edges.\nSuppressing findings is about the inline directives and path ignores.\nCustom rules shows how to extend or replace the rule set.\nLanguages and limitations lists what is supported and where the bargain shows.\nAPI reference documents every public function and type.","title":"Where to read next"},{"category":"section","location":"rules/#Custom-rules","page":"Custom rules","text":"A rule is a Rule: a metric name, its kind (:scalar or :flag), a (warn, high) band for scalars, and a function that measures one unit or the file's index. The defaults live in BUILTIN_RULES; pass rules to analyze to extend them.\n\nusing Dendro: analyze, Rule, BUILTIN_RULES\n\n# A flag rule reports one finding per node it returns from (index). The index's\n# concepts are the nodes the language query tagged. This one flags comments carrying\n# a BUG marker.\nbug_markers(index) =\n    [n for n in index.comment.nodes if occursin(\"BUG\", TreeSitter.slice(index.source, n))]\n\nanalyze(\"src\"; rules = [BUILTIN_RULES; Rule(:bug_marker, :flag, nothing, bug_markers)])\n\nA scalar rule's function is (unit, index) -> Int; its value is scored against the band and the corpus percentile like any built-in. A rule reads nodes through the index's concepts, never a node-type string, so one definition works across languages.\n\nOPTIONAL_RULES holds rules that are off by default: return_count (return points per function, which needs per-project band tuning), trivial_wrapper (a function whose body is one delegating call, which has a higher false-positive rate), and npath (NPath complexity, the count of acyclic execution paths after Nejmeh's measure as PMD computes it: sequences multiply, branches add, each &&/|| in a condition adds a path). NPath catches the sequential-branch explosion cyclomatic and cognitive complexity rate as moderate; its band wants per-project tuning, and it grows multiplicatively so the count saturates rather than overflowing. NPath is not wired for Ruby or Bash, whose branch bodies are not block nodes. local_count (distinct local names bound in a function, rebindings counted once, band 10/15 after pylint's too-many-locals) likewise wants per-project tuning. shadowed_variable flags a fresh local binding hiding an enclosing one; a Julia statement assignment in a nested scope rebinds rather than shadows and never reports, but a method local matching a class attribute does, an idiom some codebases use routinely, so the rule is opt-in. fan_out (distinct callables a function invokes, by called name with a member call counted by its final name, band 12/20 anchored at the p95/p99 of a six-corpus calibration) is opt-in because no fixed band separates a smell from a legitimate orchestrator: idiomatic corpora run p99 anywhere from 9 to 26. Opt in with analyze(path; rules = [BUILTIN_RULES; OPTIONAL_RULES]).","title":"Custom rules"}]
}
