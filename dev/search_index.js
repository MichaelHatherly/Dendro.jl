var documenterSearchIndex = {"docs":
[{"category":"section","location":"rules/#Custom-rules","page":"Custom rules","text":"A rule is a Rule: a metric name, its kind (:scalar or :flag), a (warn, high) band for scalars, and a function that measures one unit or the file's index. The defaults live in BUILTIN_RULES; pass rules to analyze to extend them.\n\nusing Dendro: analyze, Rule, BUILTIN_RULES\n\n# A flag rule reports one finding per node it returns from (index). The index's\n# concepts are the nodes the language query tagged. This one flags comments carrying\n# a BUG marker.\nbug_markers(index) =\n    [n for n in index.comment.nodes if occursin(\"BUG\", TreeSitter.slice(index.source, n))]\n\nanalyze(\"src\"; rules = [BUILTIN_RULES; Rule(:bug_marker, :flag, nothing, bug_markers)])\n\nA scalar rule's function is (unit, index) -> Int; its value is scored against the band and the corpus percentile like any built-in. A rule reads nodes through the index's concepts, never a node-type string, so one definition works across languages.\n\nOPTIONAL_RULES holds the rules that are off by default. Their bands are in Metric reference; what follows is what each measures and why it waits to be asked for.\n\nreturn_count, return points per function. Wants per-project band tuning.\ntrivial_wrapper, a function whose body is one delegating call. Higher false-positive rate than the flags that ship on.\nunreachable_after_jump, one finding per block holding a statement after an unconditional return, break, or throw.\nnpath, NPath complexity after Nejmeh's measure as PMD computes it: sequences multiply, branches add, and each &&/|| in a condition adds a path. It catches the sequential-branch explosion cyclomatic and cognitive complexity rate as moderate. The band wants per-project tuning, and the count grows multiplicatively, so it saturates rather than overflowing. Not wired for Ruby or Bash, whose branch bodies are not block nodes.\nlocal_count, distinct local names bound in a function with rebindings counted once, after pylint's too-many-locals. Wants per-project band tuning.\nshadowed_variable, a fresh local binding hiding an enclosing one. A Julia statement assignment in a nested scope rebinds rather than shadows and never reports, but a method local matching a class attribute does, an idiom some codebases use routinely.\nfan_out, distinct callables a function invokes, by called name with a member call counted by its final name. The band sits at the p95/p99 of a six-corpus calibration, and no fixed band separates a smell from a legitimate orchestrator: idiomatic corpora run p99 anywhere from 9 to 26.\n\nOpt in with analyze(path; rules = [BUILTIN_RULES; OPTIONAL_RULES]), or by name in a .dendro.toml.","title":"Custom rules"},{"category":"section","location":"tutorial/#Your-first-scan","page":"Your first scan","text":"Scan a project, read what comes back, decide what to act on, accept one finding, and turn the result into something a build can fail on. The output below comes from running Dendro over its own src, so your numbers will differ. The shapes will not.","title":"Your first scan"},{"category":"section","location":"tutorial/#Install-a-grammar","page":"Your first scan","text":"Dendro depends on no language grammars. Add the ones you intend to scan:\n\nimport Pkg\nPkg.add(url = \"https://github.com/MichaelHatherly/Dendro.jl\")\nPkg.add(\"tree_sitter_julia_jll\")","title":"Install a grammar"},{"category":"section","location":"tutorial/#Run-it","page":"Your first scan","text":"using Dendro: analyze\n\nfindings = analyze(\"src\")\n\nThat returns 440 findings on Dendro's own source and prints as a report. Nothing was built and nothing was run: Dendro parsed the files and measured the syntax.","title":"Run it"},{"category":"section","location":"tutorial/#Read-one-line","page":"Your first scan","text":"src/bindings.jl:110  owning_scope  cyclomatic 13 (warn; p99)\n\nLeft to right: where it is, the function it is about, the metric, the value, and then two separate judgements of that value. warn is the fixed band, the same threshold whatever codebase this is. p99 is the corpus percentile, this function against the other functions in the scan.\n\nTwo judgements because either one alone misleads. This line trips both, so it is an easy read. The interesting case is the line that trips only one:\n\nsrc/clones.jl:190  cluster_duplicates  function_length 41 (ok; p99)\n\nForty-one lines is unremarkable against a fixed target of fifty. It is also longer than 99% of the functions around it. Neither reading is wrong. Only the second one says this function is unusual for the code around it. Scoring and metrics is why both are kept.","title":"Read one line"},{"category":"section","location":"tutorial/#Read-the-top,-not-the-whole-thing","page":"Your first scan","text":"440 findings is not 440 problems. The report ranks by percentile and never comes back empty, because its job is triage: it puts the outliers where you will see them and lets you stop reading when the lines stop being interesting. That is the whole design. Dendro does not decide the code is wrong, it decides where to look.","title":"Read the top, not the whole thing"},{"category":"section","location":"tutorial/#Ask-what-would-fail-a-build","page":"Your first scan","text":"The report is for a human. A gate needs a question with a satisfiable answer, and that is a different call:\n\nDendro.errors(\"src\")\n\nZero, on this source. errors returns only the error-severity findings, the :high band, so a clean codebase gives back nothing and a test can assert on it. Nothing in it depends on the corpus, so it does not move when you scan a different set of files.","title":"Ask what would fail a build"},{"category":"section","location":"tutorial/#Accept-one-finding","page":"Your first scan","text":"Some flagged code is fine. Say so in the code, and the threshold stays where it is for everything else:\n\n# dendro-ignore: parameter_count\nFinding(file, line, unit, metric, value, absolute, percentile, kind, suppressed) =\n    Finding(metric, [Location(file, line, unit)], value, absolute, percentile, kind, suppressed)\n\nScan again and the report ends with a line like this one:\n\n16 finding(s) suppressed by directives\n\nSuppressed, not dropped. The count stays visible, so muting something is a decision someone can see and revisit. A misspelt metric name warns instead of quietly doing nothing. Suppressing findings covers the rest, including whole-file and by-path exclusion.","title":"Accept one finding"},{"category":"section","location":"tutorial/#Ask-about-a-change-instead-of-a-codebase","page":"Your first scan","text":"Whole-file analysis asks whether code is bad. Review asks whether an edit made it worse, which is a better question when the diff is large and generated:\n\nanalyze(\"src\"; base = \"HEAD\")\n\nOnly the functions the change touched, still scored against the whole corpus, so the baseline does not shrink to the diff.","title":"Ask about a change instead of a codebase"},{"category":"section","location":"tutorial/#Where-to-go-next","page":"Your first scan","text":"Gating CI turns errors into a test item and a pull-request annotation, and adds the ratchet that makes it adoptable on a codebase that is not yet clean.\nMetric reference lists every metric name, its default band, and whether it runs by default.\nConfiguration file retunes the bands and switches rules on and off.\nCommand line runs the same scan from a shell.","title":"Where to go next"},{"category":"section","location":"dependencies/#Dependencies-and-layout","page":"Dependencies and layout","text":"Cohesion and placement reads the graph of units referencing units. These rules read the level above it: files depending on files, contracted by directory. The resolution behind both is the same, name-based and gated by declared visibility, and every rule here stays quiet in a language with no linkage query.","title":"Dependencies and layout"},{"category":"section","location":"dependencies/#Layout-against-coupling","page":"Dependencies and layout","text":"Reported as :incoherent_package: a directory whose units mostly belong to communities anchored in other directories. Placement reads those communities per unit and scattering reads them per file; this reads them per directory, the level a repo declares its structure at. The score is the percentage of a directory's units whose community is anchored elsewhere, so a directory of forty units is comparable with one of four, where a count of the communities it touches would not be. The finding's locations pair one representative unit per elsewhere-anchored community with the unit anchoring that community, which is how the directories the contents belong to get named.\n\nThe pass proposes a rearrangement rather than a bounded edit, and it restates per directory much of what :scattered reports per file, so it is off by default and opts in through a .dendro.toml:\n\n[rules]\nincoherent_package = true\n\nMeasured over 37 corpora, 90% of directories score zero and half a percent reach the warn band, so a project that groups its code by what it couples to sees nothing here. A directory holding fewer than three units is not scored: over that few, the percentage says more about the directory's size than about its coupling. Reading the directory from the path rather than from a declared module keeps the rule the same across languages that have modules and languages that do not.","title":"Layout against coupling"},{"category":"section","location":"dependencies/#Directories-that-outgrew-one-folder","page":"Dependencies and layout","text":"Reported as :divisible_package: a directory holding groups of children that could become subdirectories. Placement, scattering and layout all ask the outward question, whether code belongs somewhere else. This asks the inward one. A directory whose contents all belong exactly where they are can still hold several independent groups that never became folders, and then the contents are right while the internal shape is missing.\n\nThe directory is read as its direct children over the file dependency graph, a child file as one node and a child directory as one node with everything under it contracted in. The communities of that induced subgraph are the candidate groups, and the score is the best one's internal ratio as a percentage: of the reference weight its members carry inside the directory, the share that stays inside the group. A group at 95 sends nineteen references in twenty to its own members.\n\nGroups are extracted, not partitioned. Whatever no folder claims stays at the top level and is expected to, so a directory of two cohesive subsystems beside a pile of miscellaneous files yields two folders and keeps the rest loose. The first location stands for the directory and its label says how much of it the proposal places, so the leftover reads off the finding. One location per proposed folder follows, each labelled with its size and its internal ratio:\n\nsrc/backend/constfold/pass.jl:1  [src/backend, 12 of 12 children placed]  divisible_package 100 (high)\n    also at src/backend/constfold/pass.jl:1  [6 children, 100% internal]\n    also at src/backend/emit/pass.jl:1  [6 children, 100% internal]\nsrc/parser/cursor.jl:1  [src/parser, 16 of 16 children placed]  divisible_package 95 (high)\n    also at src/parser/decl.jl:1  [5 children, 95% internal]\n    also at src/parser/diagnostic.jl:1  [5 children, 95% internal]\n    also at src/parser/cursor.jl:1  [6 children, 91% internal]\n\nBoth readings are in that report. src/parser holds sixteen loose files and divides into three folders. src/backend holds twelve subdirectories and no loose files at all, and its proposal groups those subdirectories under two new parents. Same score, same gates, same proposal, and only the things being moved differ. Contracting a child directory into one node is what makes one rule cover both: score a directory on its files alone and a fully-subdivided one has no nodes and is never asked the question.\n\nA finding proposes one level and stops. A group that itself wants dividing says so on the next scan, once it is a directory rather than a proposal, which terminates on its own because each level's children are fewer.\n\nSeveral gates decide whether the question applies before any ratio is read. A directory below twelve children is small enough to read at a glance and does not want subdirectories whatever its shape. A group below five children is a family rather than a folder, so eight groups of three stay silent even though they divide perfectly. A proposal placing less than a quarter of the directory leaves too much loose to be worth the move, and a lone folder covering more than seven tenths of it is a rename rather than a split. Where fewer than three quarters of the children carry any resolved reference the rule declines to score the directory at all: an unreferenced child is either genuinely independent or a reference the resolver missed, and nothing syntactic tells the two apart, so a score built on the connected part would measure the resolver rather than the code.\n\nTwo kinds of child are dropped before the groups are read. One that most of its siblings reach for is an artifact of cross-cutting coupling rather than a subsystem, and left in it pulls the whole directory into one group. One that names the directory it sits in (lib.rs, mod.rs, __init__.py, index.ts) cannot move into a subdirectory of it. Both stay at the top level, like everything else the proposal does not extract.\n\nThe pass proposes a rearrangement rather than a bounded edit, so it is off by default and opts in through a .dendro.toml:\n\n[rules]\ndivisible_package = true\n\nThe band comes from directories whose factoring is known because they were generated, not from any corpus. Three groups of eight files read 100 when nothing crosses between them, 75 at one cross-reference per file, 59 at two and 44 at four, so warn at 60 sits just above the level where groups are too entangled to separate without trading one tangle for another, and high at 85 above the level where they are nearly independent. Warn is also the bar a group clears to be proposed, so retuning the band under [bands] retunes what gets proposed with it. Dendro's own src reads 44 over six groups and stays quiet, which is the rule declining to call recognisable layers a modular decomposition. A directory deliberately kept flat is accepted with dendro-ignore: divisible_package in the file the finding reports it at.","title":"Directories that outgrew one folder"},{"category":"section","location":"dependencies/#Dependencies-against-the-grain","page":"Dependencies and layout","text":"Reported as :back_edge: a reference running against the direction two directories have settled on. Placement and scattering read the corpus as units coupling to units. This reads it one level up, as files depending on files, contracted by directory. Between any two directories, count the reference weight each way. When A -> B carries forty references and B -> A carries two, the code has established a direction, and each of those two runs against it: an import to drop and a definition to move.\n\nThe score is the dominance percent, 100 * major / (major + minor), and higher is worse. A pair at 60/40 is a genuinely mutual dependency, a cycle rather than a violated grain. A pair at 98/2 is an established layering with a few references running backwards. The layering is inferred from the corpus, so no declared layer order and no configuration is involved. Two scores as usual, the absolute band on the dominance percent and the percentile over the pairs that couple both ways at all, which is the meaningful comparison. A pair whose majority direction carries too few references has settled on no direction and is not scored, and a project whose files all sit in one directory yields no pairs at all.\n\nThe score is a ratio, so it says the direction is one-way, never that the way back is small. A pair with a large majority side clears a high band while still carrying dozens of references home, and each of those is its own finding. Read how many findings a pair produced, not the score, to judge how large the edit is.\n\nA pair spread over more than a few file edges is therefore reported without gating: the findings still name every edge, but none of them is error severity, whatever the dominance. There is no single edit to propose for such a pair, and one architectural observation should not become a dozen errors in a CI gate. It is the same treatment a dependency cycle too tangled to cut receives.\n\nOne finding is emitted per file edge in the minority direction rather than one per pair, since the edit is to an edge. Its locations are the import statement admitting the edge, where the language declares one, then every reference site across it. That is wider than a per-file metric's locations and it is deliberate: a change that adds a use of an already-imported name introduces a back edge without touching the import's line, and a finding located only at the import would drop out under base scoping. One consequence follows in the gate. errors(; since) keys a finding by its location set, so adding a reference to an established back edge re-reports it. That is the intended reading: the ratchet catches worsening, and another reference across a back edge is worsening.\n\nThe width rule runs the other way, which is worth knowing before you rely on the gate here. A change that adds one more back-referencing file, taking a pair just past the width limit, demotes every finding on that pair at once: violations that were error severity before the change stop being so, and the gate for that pair empties rather than fills. That is the width rule working, since none of those findings names a single edit any more, but it does mean this metric's gate contribution does not only ever grow as the coupling gets worse. The finding count does.\n\nDeliberate callbacks and plugin registration point backwards by design. Accept one with dendro-ignore: back_edge.","title":"Dependencies against the grain"},{"category":"section","location":"dependencies/#Dependency-cycles","page":"Dependencies and layout","text":"Reported as :dependency_cycle: a group of files that depend on one another in a loop, read off the same file dependency graph as :back_edge. The shape of the finding is the whole design here, because cycle membership on its own describes most of a real codebase. Measured over nine corpora it covered 319 of 4528 files, and 84% of the files in the worst of them. A rule that fires that broadly names no edit and takes the error floor with it.\n\nSo the finding is the feedback arc set, the edges whose removal would make the group acyclic. The same nine corpora put seven findings in the floor. Tarjan finds the groups; each group of two or more files then goes to the Eades-Lin-Smyth heuristic, weighted by reference count, which sends the heavy dependencies forward so that what points backwards afterwards is the light traffic, the cheaper edit. That heuristic bounds the size of the set it returns and runs in linear time. It does not return a minimum feedback arc set, and nothing in the report claims it does.\n\nThe score is the number of files in the group, against the absolute band and the corpus percentile over every cyclic group's size. The percentile fired on none of the nine calibration corpora: a corpus large enough to rank against carries enough small cycles that they tie low, so in practice the band carries this metric.\n\nThe locations are where the two kinds of finding part. Under a handful of cuts, each location is one edge to remove, at the import statement admitting it where the language declares one, labelled cut -> <target> with the target named relative to the source file's directory:\n\nsrc/session.jl:4  dependency_cycle 3 (warn; p80)\n    cut -> render.jl\n    also at src/render.jl:2  cut -> ../lib/token.jl\n\nAbove that many cuts the group has no bounded edit, so the locations become its highest-degree members and every label reads tangled: <n> cuts instead. Reporting the tangle rather than dropping it is the honest-over-silent call, and the label is what tells the two kinds apart without inferring anything from how many locations there are.\n\nThe cut label is part of the key the gate ratchet matches on, which is why the target is named relative to the source file rather than absolutely: errors(; since) scores the base revision in a temporary directory, and an absolute target would re-report every cut as new.\n\nA cycle that a language's build tolerates by design, a pair of mutually recursive modules, is accepted with dendro-ignore: dependency_cycle. Like every rule over the file graph, this one needs a linkage query for the language, and stays silent where there is none.","title":"Dependency cycles"},{"category":"section","location":"dependencies/#Hub-files","page":"Dependencies and layout","text":"Reported as :hub: a file that both depends on much of the corpus and is depended on by much of it, the Crossing anti-pattern. The reading comes from the file dependency graph, the same resolution placement uses one level up, with nothing filtered out: the score is min(fan_in, fan_out) over the count of distinct files depending on this one and the count it depends on. The conjunction is the whole signal. Fan-in alone is every utility module and fan-out alone every orchestrator, neither of which is a smell; a file with both is the one that propagates every change in either direction.\n\nThe finding's first location is the file, at its first unit. When the hub's externally-referenced definitions fall into two or more audiences, groups of definitions linked by sharing a consumer file, one representative definition per audience follows, each labelled with the files that consume it. That split is the proposed edit, and the labels are what say who each half is for:\n\nsrc/session.jl:1  hub 18 (warn; p97)\n    also at src/session.jl:12  open_session  [used by api.jl, router.jl]\n    also at src/session.jl:96  render_token  [used by view.jl, +3 more]\n\nA label naming more than a few consumers stands for the rest with a count, so a definition half the corpus reaches for does not turn the report into a list of paths.\n\nA group counts as an audience only with at least two definitions and two consumer files: a definition one file happens to use is ordinary, and a proposal built from singletons would name every definition its own audience. A hub left with fewer than two audiences carries no representatives: it is a warning with no proposal, and saying so beats proposing a split that does not hold.\n\nBecause the representatives sit in the finding's locations, a change to who consumes the file rewrites the key the gate ratchet matches on, and errors(; since) reports the hub again. That is intended: the split the earlier finding proposed no longer holds.\n\nTwo scores again, and here the corpus percentile does most of the work. Fan-in and fan-out both grow with the corpus, so the absolute band can only mark the level at which a crossing reads as central whatever the corpus, while the rank places one file against another. The ranked population is the files that cross at all, and a corpus below MIN_HUB_CORPUS_FILES is not scored: below it every file touches most of the others and the count says nothing about the architecture. A deliberate facade sits between two halves of a system by design; accept one with dendro-ignore-file: hub.","title":"Hub files"},{"category":"section","location":"pattern-syntax/#Pattern-query-syntax","page":"Pattern query syntax","text":"What a <lang>.patterns.scm file may contain: how a capture names a rule, which predicates are available, and how a repeated shape factors out. Pattern rules covers declaring a rule, testing one, and what the mechanism cannot reach.","title":"Pattern query syntax"},{"category":"section","location":"pattern-syntax/#Captures","page":"Pattern query syntax","text":"; A capture names a rule.\n(catch_clause) @empty_catch_binding\n\n; A `.not` capture subtracts from the rule of the same name, by node identity. Write\n; the pattern, then the more specific pattern that should be excluded.\n(catch_clause (identifier)) @empty_catch_binding.not\n\n; A `_` prefix marks a capture a predicate needed, never a rule of its own.\n((call_expression (identifier) @_f) @eval_call (#eq? @_f \"eval\"))\n\nThree conventions carried by the capture name:\n\n@rule is a match to report.\n@rule.not subtracts from @rule by node identity. Because the two patterns land on the same anchor node, negation is predictable and does not depend on where inside a match the exclusion sat. This is how a rule says \"without\": a catch binding nothing, a switch with no default, a function with no return type annotation.\n@_anything is a helper. A predicate has to reference a capture, so #eq? @_f \"eval\" forces capturing the identifier it tests; without the prefix that capture would become a rule firing on every node the predicate examined.\n\nSeveral patterns may share one capture name and accumulate, which is how one rule carries more than one spelling:\n\n((call_expression function: (member_expression object: (identifier) @_o))\n @debug_output (#eq? @_o \"console\"))\n(debugger_statement) @debug_output\n\nEvery other capture must have a [patterns.<name>] table. A capture naming no declared rule is an error at load, which is what catches a typo and a helper that lost its underscore.","title":"Captures"},{"category":"section","location":"pattern-syntax/#Predicates","page":"Pattern query syntax","text":"The predicates available are the ones TreeSitter.jl implements. Anything else is rejected when the rule loads.\n\nReading capture text: #eq?, #not-eq?, #any-of?, #not-any-of?, #match?, #not-match?, and the quantified #any-eq?, #any-not-eq?, #any-match?, #any-not-match?.\n\nReading the tree around a capture: #has-ancestor? and #not-has-ancestor? for whether some construct encloses the node, #nearest-ancestor? for which of several encloses it most closely, #ancestor-match? and #not-ancestor-match? for whether the enclosing construct's own text matches a pattern, and #has-descendant? and #not-has-descendant? for whether something sits anywhere below it.\n\nComparing two captures as code: #structure-eq? and #not-structure-eq? compare node types, child correspondence, and leaf text. Whitespace lives between tokens rather than in the tree, so formatting drops out; leaf text is compared, so a renamed operand stays a difference. That is the comparison a rule about duplicated code wants, where #eq? reads raw source and the clone passes fold identifiers out of their hashes entirely.\n\nNode properties and metadata: #is?, #is-not?, and #set!.\n\nA .not capture and a negated predicate are different tools. .not subtracts a narrower shape from the same anchor, which is how a rule says \"a catch binding nothing\" or \"a using with no selected names\". A negated predicate asks about what encloses the node or sits below it, which no shape at a shared anchor reaches. Reach for .not when the exclusion is a more specific version of the same pattern, and for the predicate otherwise.\n\nPrefer #eq? and #any-of? to #match? where either works, since a match's predicates are re-evaluated once per capture and a regex rule reruns it each time. #has-descendant? walks the subtree under a capture, so it costs the size of that subtree per match where the ancestor predicates cost its depth; a broad capture paired with it is the expensive shape.","title":"Predicates"},{"category":"section","location":"pattern-syntax/#Fragments","page":"Pattern query syntax","text":"A shape repeated across rules factors out. A ; fragment: comment defines a piece of query text and a reference splices it:\n\n; fragment: loops = [(while_statement) (for_statement)]\n\n@loops @any_loop\n(function_definition (block @loops)) @loop_in_function\n\nExpansion is one level: a reference inside a fragment is left alone rather than expanded recursively. A fragment cannot share a name with a declared rule, since a capture cannot be both a shape to splice and a rule to report.","title":"Fragments"},{"category":"section","location":"libraries-howto/#Comparing-against-a-library","page":"Comparing against a library","text":"Point Dendro at the source of a dependency and it reports where your code duplicates it. Duplication against a library explains what the two passes read and why only one of them gates; this page is how to run one.","title":"Comparing against a library"},{"category":"section","location":"libraries-howto/#Running-a-scan","page":"Comparing against a library","text":"using Dendro: analyze, Library\n\nanalyze(\"src\"; libraries = [Library(\"IterTools\", \"~/.julia/packages/IterTools/A1b2C/src\")])\nanalyze(\"src\"; libraries = [\"../shared/src\"])   # a bare path is one root, named after it\n\nAbsent libraries in both the config and the keyword, neither pass runs and the scan costs nothing. There is nothing to switch on, only something to point at.","title":"Running a scan"},{"category":"section","location":"libraries-howto/#Declaring-libraries-in-.dendro.toml","page":"Comparing against a library","text":"Declare libraries where a project declares everything else:\n\n[libraries.IterTools]\npath = \"~/.julia/packages/IterTools/*/src\"\n\n[libraries.Base]\npath = \"/usr/local/julia/base\"\nignore = [\"precompile.jl\"]\n\n[libraries.Internal]\npaths = [\"../shared/src\", \"../shared/ext\"]\n\n[clones]\nlibrary_threshold     = 0.90   # near-miss match cutoff\nlibrary_gate_coverage = 50     # coverage a public whole-unit match needs for :high\nlibrary_anchor_grain  = true   # compare blocks too, not just whole functions\n\n[rules]\nlibrary_near_duplicate = false  # keep the exact pass, drop the near one\n\npath or paths, at least one required. Relative paths resolve against the config file's own directory, ~ expands, and a single * per path component expands as a filesystem glob, which is what makes ~/.julia/packages/IterTools/*/src survive a version bump. ** is refused: one * covers the version-slug case, and recursive globbing over a package depot is a way to index gigabytes by accident.\n\nA path that matches nothing is an error, not a warning, where an unknown key inside the same table warns and is dropped. The asymmetry is deliberate: a typo'd key leaves the rest of the table working, whereas a path resolving to nothing silently turns the gate off, which is the failure this whole feature exists to prevent. The same reasoning makes a --library path that does not exist a usage error.\n\nThe thresholds live in [clones] rather than a table of their own. They are clone-detection thresholds, and a [library] table one letter from [libraries] is exactly the typo that would silently discard a setting, since an unknown top-level key warns and is dropped. Duplication against a library records the measurements each default came from.","title":"Declaring libraries in .dendro.toml"},{"category":"section","location":"libraries-howto/#From-the-command-line","page":"Comparing against a library","text":"--library, listed among the flags under Command line, is repeatable and merges with whatever the config declares. Its value splits on its first =, so a Windows path keeps its drive letter.\n\nTurning \"my dependencies\" into a list of source directories is per-ecosystem policy, and a tool that reads a dozen grammars should not carry one language's package manager. So the workflow generates the paths. For a Julia project:\n\n- name: Resolve dependency sources\n  run: |\n    julia --project=. -e '\n      using Pkg\n      open(\"dendro-libs.txt\", \"w\") do io\n          for (_, p) in Pkg.dependencies()\n              p.is_direct_dep || continue\n              src = joinpath(p.source, \"src\")\n              isdir(src) && println(io, \"--library=\", p.name, \"=\", src)\n          end\n      end'\n\n- name: Dendro\n  run: |\n    mapfile -t LIBS < dendro-libs.txt\n    dendro --check --format=github \"${LIBS[@]}\" src\n\nmapfile and the quoted expansion rather than $(cat ...): a depot under a home directory with a space in it would otherwise word-split into flags naming nothing, and a --library naming nothing is the silent clean run this design refuses.\n\nDirect dependencies only. Indexing the transitive closure finds matches against packages the project cannot import without adding a dependency, which is a different and much weaker finding. Dendro has no way to tell the two apart, so the recipe draws the line rather than the tool.","title":"From the command line"},{"category":"section","location":"libraries-howto/#The-gate-and-the-ratchet","page":"Comparing against a library","text":"Dendro.errors(\"src\"; libraries = [...], since = \"origin/main\")\n\nNothing in the ratchet key mentions a library, which has three consequences, each a decision rather than a side effect. Upgrading a dependency never re-reports an unchanged finding. Upgrading a dependency can introduce one, when the library grows a function you already wrote, and that reads as new, which is right: something you should now import did not exist before. And the base revision is scored against the libraries as they are on disk now, so the ratchet holds them fixed and asks what your change did.\n\nTurning the pass on cold will produce findings. since is what makes that survivable, as it is for every other rule. See Gating CI.","title":"The gate and the ratchet"},{"category":"section","location":"libraries-howto/#What-it-costs","page":"Comparing against a library","text":"Indexing the libraries is the dominant term and scales with their source size, not with your project's. Measured cold on six projects with 40 to 360 dependency source files: 0.3 to 1.5 seconds to index, and 0.8 to 3.8 seconds for a whole scan. The exact join is one dictionary lookup per project anchor, so it is independent of how large the libraries are, which is what lets it gate.\n\nIndexing memoizes to disk in a scratch space Dendro owns, keyed by the index format, the Dendro and Julia versions, min_size, the grain, the size and mtime of every file indexed, and the query files of each language among them. A dependency set changes rarely, so a warm cache turns that 0.3–1.5 seconds into 0.06–0.15. Any load failure at all (a stale format, a truncated write, a file another tool left there) is a miss and a rebuild. A cache is an optimisation and must not be able to break a scan, so there is no error path from it.\n\nThe queries are in the key because they decide what an anchor is. Retune one with [languages.julia] queries and the set of files indexed does not change, so without them the key would not move and you would be served an index built against the query you replaced.\n\nEntries are written in a format Dendro defines rather than through Julia's Serialization. Serialization rebuilds whatever types a stream names, which makes reading an entry someone else wrote a way to run their code; the reader here names no type and allocates nothing from a length it has not checked against the bytes in front of it. That matters if you share a cache directory between CI jobs, which is the usual way to keep one warm.\n\nSet DENDRO_CACHE_DIR to put the indices somewhere else, on a machine whose depot sits on a disk that cannot hold them. It is an environment variable and not a .dendro.toml key because the config file carries opinions about your code, not paths on your machine. Point it at a directory Dendro owns: collection deletes entries by name, and while it only ever removes names that look like its own hex digests, a directory you also keep other things in is not what it is for.\n\nAn entry no scan has used for a week is deleted, swept when a new index is written and at most once a day. This matters more than it sounds: the cache key includes each indexed file's size and mtime, so upgrading a dependency does not overwrite its entry, it writes a new one beside it. Without collection the space would grow for as long as your dependencies keep moving. Scratch spaces are reclaimed by Pkg.gc() only once the owning package is uninstalled, which is too late to be the answer here.\n\nTo clear the lot by hand:\n\nusing Scratch, Dendro\nScratch.clear_scratchspaces!(Dendro)","title":"What it costs"},{"category":"section","location":"libraries-howto/#When-the-finding-is-wrong","page":"Comparing against a library","text":"Answer with a suppression, not by arguing with the tool. dendro-ignore: library_duplicate on the site's line, read exactly as :duplicate is; the finding stays counted and visible.\n\n# dendro-ignore: library_duplicate -- LICENCE forbids taking the dependency\nfunction chunk_by(xs, n)\n\nThe cases worth recognising:\n\nInterface boilerplate. Base.show, iteration protocol methods, table glue. Two packages implement the same protocol the same way because the protocol says to. The doubled floor for control-free functions removes the smallest of these.\nYour project is a fork of, or an ancestor of, the library. Every function matches. A dendro-ignore-file, or drop the library from the config.\nDeliberate avoidance. A licence, a heavy dependency being trimmed, a hot path where the library's generality costs too much. Suppress with the reason in the comment, which is what an inline directive is for.\nTest helpers. Pointing a library at its test/ finds fixture builders everywhere. Configure src only, and ignore for the rest.\nGenerated code. Both corpora generated from the same schema. ignore patterns, the same answer whole-corpus scanning already gives.\nOverlap with :duplicate. Two copies in your code of something a library also has fires :duplicate once over the cluster and :library_duplicate at each site. Both stay: they name different edits, unify the two copies, or delete both and import.","title":"When the finding is wrong"},{"category":"section","location":"metric-reference/#Metric-reference","page":"Metric reference","text":"Every metric Dendro reports, its default band, and the page that explains what it measures. A name in these tables is what an inline dendro-ignore accepts, what [rules] switches on or off, and what [bands] retunes.\n\nA band is a (warn, high) pair. A value at or above warn reports :warn, a value at or above high reports :high, and only :high reaches the floor errors gates on. A flag carries no band: it reports at a fixed severity, :high for every rule Dendro ships.","title":"Metric reference"},{"category":"section","location":"metric-reference/#Per-function-scalars","page":"Metric reference","text":"Scored against the band and the corpus percentile, and flagged when either fires. See Scoring and metrics.\n\nmetric band default measures\ncyclomatic 11, 21 on decision points in a unit\ncognitive_complexity 15, 25 on decision points weighted by the nesting they sit under\nfunction_length[1] 50, 100 on lines in a definition\nnesting_depth 4, 6 on deepest nesting reached\nparameter_count[1] 5, 8 on parameters in a signature\nboolean_complexity 4, 6 on most &&/`\nreturn_count[1] 4, 8 opt-in return points in a definition\nnpath 200, 1000 opt-in acyclic execution paths\nlocal_count[1] 10, 15 opt-in distinct local names bound\nfan_out 12, 20 opt-in distinct callables invoked\n\n[1]: Measures a definition only. Top-level code has no signature and no author-drawn boundary, so these stay quiet there.","title":"Per-function scalars"},{"category":"section","location":"metric-reference/#Per-function-flags","page":"Metric reference","text":"Presence is the finding. Every one reports :high, so every one reaches the gate.\n\nmetric default flags\nidentical_operands on x == x, a && a\nduplicate_branches on a conditional whose branches are all identical\nempty_body on a definition whose body does nothing\nempty_catch on a catch clause that discards the error\nstub_marker on a TODO/FIXME/XXX/HACK comment\nreturn_in_finally on a return inside a finally clause\nunused_parameter on a parameter nothing in the unit references\nunused_local on a local binding nothing in the unit references\nbroad_catch on a handler that swallows interrupts and exits\ntrivial_wrapper opt-in a body that is one delegating call\nunreachable_after_jump opt-in code after an unconditional return, break, or throw\nshadowed_variable opt-in a local binding hiding an enclosing one","title":"Per-function flags"},{"category":"section","location":"metric-reference/#Computed-across-the-corpus","page":"Metric reference","text":"The band column is empty where the metric is a flag and always reports :high.\n\nmetric band default value is explained in\nduplicate  on members of the clone cluster Duplicate detection\nnear_duplicate  on the cluster's weakest pairwise similarity, percent Duplicate detection\nreimplementation  opt-in shared rare vocabulary, percent Duplicate detection\nunnatural 400, 500 on cross-entropy in centibits[2] Scoring and metrics\nlow_cohesion 4, 6 on independent concerns sharing a file Cohesion and placement\nscattered 7, 10 on modules the file's units are pulled toward Cohesion and placement\nsplit_audience 3, 5 on consumer groups the file serves Cohesion and placement\nmisplaced 60, 80 on coupling landing in one other file, percent Cohesion and placement\ndistant_definition 25, 50 opt-in definitions between it and its nearest use Cohesion and placement\nunreferenced  on nothing; the definition is the finding Cohesion and placement\nback_edge 85, 95 on dominance of the directory pair, percent Dependencies and layout\ndependency_cycle 5, 10 on files in the cyclic group Dependencies and layout\nhub 15, 30 on min(fan_in, fan_out) over distinct files Dependencies and layout\nincoherent_package 50, 75 opt-in the directory anchored elsewhere, percent Dependencies and layout\ndivisible_package 60, 85 opt-in the best group's internal ratio, percent Dependencies and layout\n\n[2]: Hundredths of a bit per token, so the default band is 4.00 and 5.00 bits. The value is rounded for reporting; the percentile ranks on the unrounded score.\n\nThe eleven with a band are the relational names [bands] accepts. The rest carry no band to retune.","title":"Computed across the corpus"},{"category":"section","location":"metric-reference/#Against-a-library","page":"Metric reference","text":"Both are opt-in and both need a library to compare against. See Duplication against a library.\n\nmetric default value is band\nlibrary_duplicate opt-in coverage of your function, percent :high for a public whole-function match at or above library_gate_coverage, else :warn\nlibrary_near_duplicate opt-in coverage of your function, percent always :warn, so it never gates","title":"Against a library"},{"category":"section","location":"metric-reference/#Names-a-project-adds","page":"Metric reference","text":"A [patterns.<name>] table declares a rule whose name joins this namespace, and a Rule passed to analyze does the same. Either suppresses, toggles and retunes exactly as a built-in does. See Pattern rules and Custom rules.\n\nA name Dendro does not know warns wherever it appears, in a dendro-ignore directive or in a [bands] or [rules] table, so a typo is visible instead of silently disabling a check.","title":"Names a project adds"},{"category":"section","location":"cohesion/#Cohesion-and-placement","page":"Cohesion and placement","text":"Readings of the graph of units referencing units: whether a file's functions group by usage, whether a unit sits in the file it belongs in, whether a file's units are pulled apart, who outside the file consumes what it defines, and whether a private definition is reached at all. The level above, files depending on files, is Dependencies and layout.","title":"Cohesion and placement"},{"category":"section","location":"cohesion/#Within-file-cohesion","page":"Cohesion and placement","text":"analyze reports files whose functions split into independent concerns, as :low_cohesion. It builds a graph of a file's functions and links two when they reference a common file-local name, a helper, type, or constant defined in the same file. A file that breaks into several disconnected components holds that many concerns living together, the LCOM4 reading of low cohesion. The finding's value is the component count, and its locations are one representative function per component:\n\nsrc/util.jl:1  parse_date  low_cohesion 3 (warn)\n    also at src/util.jl:40  render_html\n    also at src/util.jl:88  open_socket\n\nTo link on the name's binding rather than the bare string, Dendro resolves each reference to the definition it refers to within the file (tree-sitter locals-style scopes, src/queries/<lang>.scopes.scm). This drops the noise a string graph carries: a local x in one function and a same-named x in another are different bindings, and an imported or builtin name resolves to nothing. A binding referenced by most of the file's functions is a cross-cutting utility, not a shared concern, and links nothing. Reported with both scores, the absolute band on the component count and the corpus percentile.\n\nThe resolution is lexical, never dispatch: an edge means two functions reference the same file-local name, not that they call the same method. The signal stays syntactic and within one file. Cohesion runs for every supported language; each ships a scopes query.\n\nThe lexical line has a cost in class-based code. An edge is call linkage, two functions naming the same file-local definition, not shared-field cohesion: with no symbol or field resolution, methods that touch the same instance field through different names form no edge. The reading is weakest for a file that is one class, where field-sharing is the main cohesion and Dendro sees only method-to-method calls. Java is the extreme, since every file is one class.","title":"Within-file cohesion"},{"category":"section","location":"cohesion/#Cross-file-placement","page":"Cohesion and placement","text":"Reported as :misplaced: a unit that couples more to another file than to its own. The within-file binding resolver leaves a reference unbound when its definition lives in another file. Placement resolves those references corpus-wide. A per-language linkage query (src/queries/<lang>.imports.scm) tags how files see each other's names across four models: a splice joins included files into one namespace (Julia include, C #include, Ruby require_relative); an import brings named definitions of a resolved module in (Python, JavaScript, TypeScript, Rust, PHP); a directory shares a package's names across its files (Go); a package adds, on top of imports, the same-directory types a language resolves without an import (Java). A reference that leaves its file resolves to the definition it names in a file its linkage exposes, and the result is a corpus-wide graph of which unit references which.\n\nA splice carries namespaces in some languages. A definition inside a Julia module that an included file declares does not join the includer's namespace: nothing reaches it by bare name, only by qualifying it (Mod.f). Such a definition resolves under its qualified name, matched on the namespace enclosing it directly, since the outer modules a reference walks through can be declared in a file the definition's own module path never sees.\n\nA definition at file scope answers to both forms. Its file was spliced into whatever module the include sits inside, a namespace the file itself never declares, so the corpus walks the splice chain to find the module path each file lands in. A file-scope name is then visible bare and qualified, and so is a name the module's own body declares, read from the file it includes: the two sit in one namespace, so a reference there writes the bare name. Reading a qualified reference this way also stops a field read (row.total) resolving as a bare total: the two are the same syntax, and a value's field matches no namespace.\n\nThe score is the envy percent, the share of a unit's whole coupling, own-file and cross-file, that lands in the single other file it leans toward most. A unit devoted to one other file scores near 100; a coordinator that reaches into several files spreads its mass and stays low. The finding's first location is the unit, its second the suggested home. Two scores, like cohesion: the absolute band and the corpus percentile, fired when either trips. The deciding gate is the graph's communities (neighbourhoods, by modularity optimisation): a unit is a candidate only when its community is anchored in a file other than its own, the module the references say it belongs to.\n\nResolution is name-based and gated by declared visibility, never typed. A reference matching several visible definitions splits its weight across them rather than picking one by dispatch. A definition many units reach for is discounted as infrastructure, so a shared helper does not pull every caller toward its file, the corpus analog of the cohesion ubiquity cut. A language with no linkage query contributes no cross-file edges.\n\nReported as :scattered: a file whose units belong to several different modules, the cross-file companion to within-file :low_cohesion. The corpus graph holds only cross-file edges, so its communities alone would split every layered file. Folding each file's within-file binding edges, the same edges cohesion links on, into the graph first lets a cohesive file's units settle into one community, so only a file whose units are each drawn toward a different other file scatters. Every unit sharing one file-local definition is joined to every other, and an edge's weight is how many definitions its two units share, so calling one helper repeatedly is not read as more coupling than calling it once. The score is the count of distinct communities the file's units occupy whose plurality anchor is another file: a file that stays home scores zero. A bag of unrelated functions is low-cohesion but not scattered, each its own self-anchored community; what scatters is a file each of whose units belongs with a different other file. Two scores, like cohesion: the absolute band and the corpus percentile.\n\nThe finding's locations are one representative unit per elsewhere-anchored community, each labelled with the file that community is anchored in. The count is the score; the labels are the edit:\n\nsrc/units.jl:11  units  [belongs with reimplementation.jl]  scattered 5 (ok; p97)\n    also at src/units.jl:19  unit_span  [belongs with flags.jl]\n    also at src/units.jl:30  unit_node  [belongs with flags.jl]\n    also at src/units.jl:39  is_callable  [belongs with corpus_graph.jl]\n    also at src/units.jl:52  is_function  [belongs with metrics.jl]\n\nTwo units pulled toward the same file is the useful reading there: it says where the seam between the two files is currently drawn wrongly. Recovering that without the labels means rebuilding the corpus graph by hand, which is work the pass has already done.","title":"Cross-file placement"},{"category":"section","location":"cohesion/#Within-file-placement","page":"Cohesion and placement","text":"Reported as :distant_definition: a definition sitting a long way from the code that uses it, the within-file companion to :misplaced. Where placement asks which file a unit belongs in, this asks where in the file a definition belongs. The score is how many top-level definitions lie between a definition and the nearest unit in its file that references it, so a definition written beside a use scores zero. The first location is the definition, the second the use nearest it.\n\nsrc/linkage.jl:60  is_type_kind  distant_definition 57 (high; p99)\n    also at src/linkage.jl:830  file_symbols!  [nearest use of is_type_kind]\n\nNearest rather than mean or median, and that choice is what keeps the rule quiet on a file-wide helper without a ubiquity cut. A name most of the file reaches for has a use close by wherever it sits, so it scores low on its own; a median would score it by the distance to the middle of the file and report every such helper. What survives is a definition whose uses all sit together somewhere else, which is the case with a home to move to. Distance counts definitions rather than lines, because a reader crossing one 200-line function has crossed one thing, and function_length is the rule that reads the 200.\n\nThe rule is off by default, and the reason is measurement rather than caution. Over 5798 scored definitions in nine corpora, half sit within one definition of a use, but the tail is long: the pooled p95 is 28 and the per-corpus p95 runs from 12 to 44. Reading every definition this package separates by 4 to 15 by hand found ordinary declaration order throughout, a helper or a documented constant written above the one function that reads it. Nothing syntactic separates a helper hoisted for reading order from one stranded by an edit, so the size of the gap is all there is to read, and the default band marks only what is beyond argument. Turn it on and set the band to the convention the project keeps:\n\n[rules]\ndistant_definition = true\n\n[bands]\ndistant_definition = [5, 10]","title":"Within-file placement"},{"category":"section","location":"cohesion/#Audience-splits","page":"Cohesion and placement","text":"Reported as :split_audience: a file whose definitions serve two or more groups of consumers that never overlap, the outward dual of :low_cohesion. Cohesion reads inward and splits a file by the bindings its own functions share; this reads outward and splits it by the files that consume its definitions. The two are independent: a file can share helpers throughout, so it reads as cohesive, while serving two audiences that never meet.\n\nFor each definition something outside the file references, Dendro collects the consumer files, links two definitions whose consumer sets meet, and takes the connected components of that graph as the file's audiences. The score is the number of audiences holding at least two definitions, so a helper with a single caller is not one. The band is [3, 5]: across ten measured corpora 85% of scored files serve a single audience and 5% serve three, so three separated interfaces is the tail and five is rare enough to gate on. Two audiences is common enough that the corpus percentile carries it. The locations are one representative definition per audience, each labelled with the files consuming that audience, which is the split the finding proposes. A file serving a single audience names no split, so it is never reported however unusual it is for the corpus, and it still counts toward the distribution the percentile reads.\n\nThe audience comes from resolved references, not from declared exports. A language with no export marker (Python, Go, C) exposes every top-level name, so an export-counting reading would collapse into file size. A file fewer than two other files consume has one audience by construction and is not scored at all.","title":"Audience splits"},{"category":"section","location":"cohesion/#Unreferenced-definitions","page":"Cohesion and placement","text":"Reported as :unreferenced: a private top-level definition no path reaches from the corpus's public surface. Dead code needs reachability, not a caller count, so a private cluster that only calls itself is still dead. The pass builds a reference graph over every top-level definition and walks forward from the roots. A definition is a root when it is declared public or referenced from top-level code, which runs unconditionally. The edges come from two sources, neither discounted: each file's within-file bindings, the same data cohesion reads, and the cross-file references placement resolves. A definition many units reach for is maximally alive, so unlike placement this graph drops no cross-cutting utility and keeps definitions that are not functions.\n\nThe public surface is per language. A name in a file's export/public list is public in Julia and JavaScript/TypeScript; a Python name is public unless it leads with an underscore; a Go name is public when it is capitalised. A per-definition visibility modifier covers the rest: a Rust item is public when it is pub, a C or C++ function is private when it is static (file-local), a method is private under a Ruby private/ protected declaration or a Java or PHP private keyword, and a Java class is private when it is package-private (not marked public). A reference is attributed to its enclosing top-level definition by byte range, so a call inside a nested helper or a lambda still keeps the enclosing function alive. A name matching several definitions keeps all of them alive, since name resolution cannot tell a type from its constructor or one method from its overload.\n\nThe reading is name-based and lexical, like the rest of placement: it matches a name to a declared definition, never resolving a type or a dispatch. Two limits follow. It is sound only over a whole module, so a private definition called from a same-module file outside the scan is falsely flagged. Runtime-only entry points (a test function, a dispatch-table callback, a string-dispatched name) carry no syntactic reference, so they are flagged unless declared public or referenced from top level; accept one with dendro-ignore: unreferenced. Java resolves a same-package reference through its :package linkage, so a package-private class with no user in its package is flagged alongside a private method. A package-private member stays public, reached same-package through a receiver the resolver does not follow. PHP checks only a private method; its classes carry no package-private privacy.","title":"Unreferenced definitions"},{"category":"section","location":"duplicates/#Duplicate-detection","page":"Duplicate detection","text":"analyze reports duplicates as part of a full analysis: code duplicated across the corpus, including across different files. It hashes each subtree by its structure, type not text, so fragments that differ only in variable names or literal values still match (Type-2 clones). It catches the copy-paste-then-rename that generated code produces. Detection works at two scales from the same mechanism: a whole function duplicated, or one block, an if or loop body, copied between functions that otherwise differ. A maximality filter keeps only the largest clone, so a duplicated function is reported once, not again for every block inside it. Each cluster of two or more comes back as one :duplicate finding whose locations list every member:\n\nsrc/a.jl:10  parse_header  duplicate 3 (high)\n    also at src/b.jl:42  read_header\n    also at src/c.jl:7  load_header\n\nmin_size (default 10 named nodes) gates out trivial functions, so one-line getters do not cluster. Blocks must clear twice that, since a short block of boilerplate coincides across unrelated code while a small whole function is already a meaningful unit. Pass min_size lower to widen the net, higher to focus on large clones.","title":"Duplicate detection"},{"category":"section","location":"duplicates/#Near-misses","page":"Duplicate detection","text":"Exact clustering misses the copy-paste-then-edit: two functions identical but for an added or removed statement hash differently and never meet. analyze also reports these as :near_duplicate. It compares each function's pre-order sequence of subtree hashes by longest common subsequence, after NiCad, scoring similarity as |LCS| / max(|a|, |b|), so functions that are close but not identical cluster when they clear the threshold (default 0.85). The LCS is order-aware where a multiset overlap is not: a reordering of the same statements, or a small fragment matching inside a large function, scores low and is rejected. A characteristic-vector radius query (DECKARD-style), banded by function size, finds candidate pairs without comparing every pair; the LCS similarity confirms each one. The finding's value is the cluster's weakest pairwise similarity as a percent:\n\nsrc/parser.jl:40  read_header  near_duplicate 88 (high)\n    also at src/loader.jl:12  load_header\n\nA near-miss stays syntactic and within one language, like exact detection. Pass threshold higher to demand closer matches, radius_factor to widen or narrow the candidate search.","title":"Near-misses"},{"category":"section","location":"duplicates/#Reimplementation-candidates","page":"Duplicate detection","text":"Both structural passes miss the rewrite: a helper reimplemented with different structure, a loop against straight-line code, shares no subtree shape with the original. It usually keeps the vocabulary, the same callees and the same domain words in its identifiers, and that is what the :reimplementation pass reads. Each function is fingerprinted by its callee names plus the word fragments of its identifiers, every term weighted by rarity in the scanned corpus (IDF, rebuilt on each scan). Two functions pair when the rare vocabulary they share outweighs the rest of their combined vocabulary, the finding's value that overlap as a percent:\n\nsrc/fetch.jl:12  fetch_once  reimplementation 74 (high)\n    also at src/retry.jl:30  fetch_with_retry\n\nThe evidence is vocabulary, not structure, so the finding is a proposal: two functions that talk about the same things in different shapes. The pass is off by default and opts in through a .dendro.toml:\n\n[rules]\nreimplementation = true\n\n[reimplementation]\nthreshold = 0.6   # overlap a pair must reach, the one knob\n\nFour gates keep the proposals worth reading. A pair the clone passes already report is theirs, never repeated here. A pair where one function calls the other by name is a caller and its helper, not a rewrite. Same-named functions never pair, since overloads and interface implementations share vocabulary legitimately. And a pair whose sizes differ beyond a 2:1 ratio is a fragment against a whole. Deliberately parallel families still pair, per-language implementations of one operation are vocabulary twins by design, so expect a codebase with such families to surface them; suppress with dendro-ignore: reimplementation where they are intended.\n\nThe pass stays name-based and corpus-derived, like the rest of Dendro: no types, no call graph, no pretrained model. A rewrite that also renames the domain vocabulary stays invisible to it.","title":"Reimplementation candidates"},{"category":"section","location":"duplicates/#Ranking","page":"Duplicate detection","text":"All three passes come back ordered by how much of the system a duplicate implicates. Dendro contracts the corpus by directory, groups directories into communities by how they couple, and reads each cluster's spread: within one file, within one directory, within one community of directories, or across communities. Widest spread leads. Two copies in one file are a tidy-up one reader makes in one sitting; two copies either side of a community boundary are a missing abstraction that unrelated parts of the codebase each built.\n\nThis is ordering only. A cluster's value, its band, and its locations are the same either way, so a duplicate is neither promoted into nor demoted out of the floor errors gates on. Inside one distance each pass keeps its own order: cluster size for :duplicate, similarity for :near_duplicate, overlap for :reimplementation.","title":"Ranking"},{"category":"section","location":"cli/#Command-line","page":"Command line","text":"julia -m Dendro <path>... runs the same analysis from a shell, in an environment where Dendro and the grammars it needs are installed. Installed as an app, it is the dendro command.\n\njulia -m Dendro src                       # report the findings\njulia -m Dendro --base=origin/main src    # only the lines a change touched\njulia -m Dendro --check src               # exit 1 on any error-severity finding (CI gate)\njulia -m Dendro --format=github src       # GitHub Actions annotations\njulia -m Dendro --library=IterTools=../IterTools/src src   # duplication against a library\n\nThe default report ranks every function by percentile, so it is never empty, the triage view. --check instead gates on the :high floor, the error-severity findings, so a clean codebase exits 0 and a regression exits 1. That is the Gating CI floor read from a shell.\n\n--check alone reports every error-severity finding in the tree, so a codebase that is not yet clean fails from the first run. --since=<ref> makes it the ratchet instead: that floor minus what the ref already reported, so only what a change introduced fails the build. It is the errors keyword of the same name.\n\ndendro --check --since=origin/main src\n\n--base asks a different question, scoping the report to the lines a change touched, so naming both is a usage error rather than a silent choice of one.\n\nBoth read the repository through libgit2 rather than by running git, so neither needs the git or tar binaries on PATH. That matters in a minimal CI container, and it means the base revision is read straight from the object database: no worktree is touched and no index is mutated. One consequence is worth knowing if your repository uses export-ignore gitattributes, which git archive would honour and a direct read of the tree does not. The base corpus is now the same set of files the working tree is scanned as, which is the comparison a ratchet wants; an export-ignored file that used to be missing from the base alone would have read as newly added.","title":"Command line"},{"category":"section","location":"cli/#Flags","page":"Command line","text":"--base=<ref>          report only findings on lines changed against <ref>\n--since=<ref>         report only error-severity findings <ref> did not already have\n--config=<file>       read <file> instead of a discovered .dendro.toml\n--no-config           ignore .dendro.toml, score against built-in defaults\n--cut=<float>         percentile cutoff for corpus-relative flags (default 0.95)\n--library=<path>      index <path> as a library to report duplication against\n--library=<name>=<path>\n--format=<fmt>        output format: text (default) or github\n--check               exit 1 on any error-severity finding\n--check-patterns      check pattern rules against their fixtures and exit\n--version             print version and exit\n--help, -h            print this message and exit\n\n--library is repeatable and merges with whatever the config declares; see Comparing against a library for the recipe that generates one flag per dependency. --check-patterns runs the fixture check described in Pattern rules.\n\nA bad flag, a missing value, or a --library path that does not exist is a usage error rather than a silent clean run.","title":"Flags"},{"category":"section","location":"cli/#Threads","page":"Command line","text":"Analysis parallelises across threads on a large corpus. Start Julia with -t auto (or -tN, or set JULIA_NUM_THREADS) and the parsing, scoring, duplicate, and cross-file passes fan out over the available threads.\n\njulia -t auto -m Dendro src\n\nEach pass falls back to serial below a small size floor on its own item count (files, clone pairs, functions), or single-threaded, so a small diff or a single-file gate pays little to no overhead. The findings are identical whatever the thread count.\n\nThe same holds for analyze called from Julia: threading is a property of the session, not a keyword.","title":"Threads"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"Dendro exports nothing. Its public API is marked with the public keyword. Import what you call, or qualify with Dendro..","title":"API reference"},{"category":"section","location":"api/#Analysis","page":"API reference","text":"mermaid is documented with the graphs it draws, under Diagrams.","title":"Analysis"},{"category":"section","location":"api/#Gating","page":"API reference","text":"","title":"Gating"},{"category":"section","location":"api/#Findings","page":"API reference","text":"","title":"Findings"},{"category":"section","location":"api/#Rules","page":"API reference","text":"","title":"Rules"},{"category":"section","location":"api/#Pattern-rule-types","page":"API reference","text":"","title":"Pattern rule types"},{"category":"section","location":"api/#Configuration","page":"API reference","text":"","title":"Configuration"},{"category":"function","location":"api/#Dendro.analyze","page":"API reference","text":"analyze(path; base=nothing, cut=nothing, min_size=nothing, threshold=nothing, radius_factor=nothing, language=nothing, rules=nothing, ignore=String[], config=nothing, libraries=nothing) -> Findings\nanalyze(paths::AbstractVector; ...) -> Findings\n\nAnalyze the file or folder at path. Every function gets scalar and flag metrics; functions duplicated across the corpus are reported as :duplicate findings, and functions that are close but not identical as :near_duplicate. A baseline is built from the corpus, the folder's files or the single file, so relative scoring works against the input's own distribution with no setup. With base, only functions changed against that git ref are reported, scored against the full-corpus baseline.\n\nPassing several paths folds their files into one corpus, so a package's src and ext are scanned together (analyze([\"src\", \"ext\"])) without dragging in the rest of the tree. The baseline, duplicate detection, and naturalness span the roots, so a function copied from one into another is caught. With base, all roots resolve to the one git toplevel and the repo-wide diff scopes them.\n\nthreshold is the LCS-similarity cutoff for a near-miss, radius_factor scales the candidate-search radius to a function's size.\n\nThe :reimplementation pass, vocabulary-overlap pairs the structural passes miss, is off by default and enabled only through the config, [rules] reimplementation = true in a .dendro.toml or a config built here; the rules kwarg carries Rules and cannot express it. Its overlap cutoff is the config's [reimplementation] threshold.\n\nThresholds come from a Config: the bands, the percentile cut, and which rules are active. By default analyze discovers one, merging a user-global config and the repo .dendro.toml over the built-in defaults. Pass config to supply one directly and skip discovery. An explicit cut or rules overrides the config, so a caller keeps the final say.\n\ncut is the percentile cutoff a corpus-relative metric flags above; it defaults to the config's, 0.95 absent a file. rules is the active rule set; absent, it is the config's resolution of BUILTIN_RULES and the enabled OPTIONAL_RULES. Pass your own to lint for a project's structural conventions: analyze(path; rules = [BUILTIN_RULES; my_rule]).\n\nlibraries names reference corpora to compare the project against, source Dendro reads and never scores: a Library each, or a bare path taken as one root. Project code a library already implements is reported as :library_duplicate and :library_near_duplicate at the site in the project, with the library symbol to import named in the label. The keyword overrides whatever the config's [libraries] tables resolved. Absent both, neither pass runs and the scan costs nothing.\n\nignore is a list of gitignore-style patterns, matched against each path relative to a scanned folder. Matching files are dropped before parsing, so vendored or generated source is neither flagged nor counted in the baseline: analyze(path; ignore = [\"vendor/\", \"*.generated.jl\"]). A leading ! re-includes, a trailing / matches directories only. As in gitignore, a file under an excluded directory cannot be re-included. Patterns apply to folder scans, not a single named file. A .dendro.toml declares the same patterns under its top-level ignore key, which is the route a command-line scan has; the keyword adds to those.\n\n\n\n\n\n","title":"Dendro.analyze"},{"category":"function","location":"api/#Dendro.active","page":"API reference","text":"active(findings) -> Findings\n\nThe findings not suppressed by an inline directive. Use this for gating.\n\n\n\n\n\n","title":"Dendro.active"},{"category":"function","location":"api/#Dendro.github_annotations","page":"API reference","text":"github_annotations(io, findings)\ngithub_annotations(findings)\n\nWrite findings as GitHub Actions workflow commands, one ::error/::warning line per finding, anchored at its first location. GitHub records each as a pull-request check annotation; it renders inline on the diff when the anchored line is part of the change, otherwise in the run's Checks tab. Pair this with analyze's base to scope findings to the functions a change touched. Suppressed findings are omitted. High-band findings map to ::error, the rest to ::warning.\n\n\n\n\n\n","title":"Dendro.github_annotations"},{"category":"function","location":"api/#Dendro.errors","page":"API reference","text":"errors(paths; since=nothing, config=nothing, rules=nothing, ignore=String[], language=nothing, libraries=nothing) -> Findings\n\nThe error-severity findings over paths: the deterministic floor, every finding at the :high absolute band (high-band scalars and all flags), with inline dendro-ignore directives applied first so a suppressed finding lifts the gate.\n\nThis is the gate companion to analyze. analyze ranks by corpus percentile for triage and so is never empty; errors reads only the fixed bands, so it is satisfiable and stable, suitable for a CI gate. Assert isempty(errors(path)) in a test and every package's existing Pkg.test() gates on Dendro for free.\n\nLike analyze, errors honors a Config: it discovers one from the repo .dendro.toml unless a config is passed, so a project that retunes a band or toggles a rule sees the change in the gate, not only in analyze. The same config scores the working tree and the since base, so a retuned band never reads as a regression on its own.\n\nWith since, a git ref, the result is the ratchet: the floor at the working tree minus the floor at that ref. A finding the change introduced is reported; one that predates the ref, even on a line the change touched, is not. This answers \"did this change introduce a violation\", and supports incremental adoption on a codebase that is not yet clean. A since that names no commit throws: a broken ref is CI misconfiguration, never a silent fall-back to the floor.\n\nsince is distinct from analyze's base. base is spatial, restricting findings to changed lines for annotations; since is a finding-set difference, the gate.\n\nconfig, rules, ignore, and language pass through to analyze; rules absent, the active set is the config's resolution of BUILTIN_RULES and the enabled OPTIONAL_RULES. libraries names the reference corpora to compare against, folded into the resolved config so the working tree and the since base are scored against the same ones.\n\n\n\n\n\n","title":"Dendro.errors"},{"category":"type","location":"api/#Dendro.Finding","page":"API reference","text":"Finding\n\nOne reported issue over one or more locations. Per-file metrics fire at a single location; relational metrics like :duplicate span several.\n\nmetric: which metric fired (:cyclomatic, :empty_catch, :duplicate, ...).\nlocations: every site the finding covers, at least one.\nvalue: the scalar reading, the member count for :duplicate, or nothing.\nabsolute: :ok/:warn/:high band, always :high for flags.\npercentile: corpus rank in [0, 1], or nothing when no corpus sample ranks against it.\nkind: :scalar or :flag.\nsuppressed: whether an inline directive accepted this finding.\n\n\n\n\n\n","title":"Dendro.Finding"},{"category":"type","location":"api/#Dendro.Findings","page":"API reference","text":"Findings <: AbstractVector{Finding}\n\nThe result of analyze: the findings it produced, printed as a report. Behaves as an AbstractVector{Finding}, so it iterates, filters, and indexes like any vector of Findings.\n\n\n\n\n\n","title":"Dendro.Findings"},{"category":"type","location":"api/#Dendro.Location","page":"API reference","text":"Location\n\nA code site: its file path, 1-based line, enclosing unit name (\"\" when no name node is found), and an optional label, what this site means to the finding carrying it.\n\nA label is what lets a finding name an edit rather than only score one. :scattered counts how many communities a file's units are pulled into; the label on each location says which file it is pulled toward, which is the move. :split_audience and :hub label each representative with the files consuming its audience, which is the split. Without them a reader has to rebuild the corpus graph to recover what the pass already computed.\n\nA label is evidence, not identity: the gate's fkey reads file and unit alone, so labelling a site never moves a ratchet key and never re-reports a finding. Where the label is the identity, :dependency_cycle's choice of which edge to cut, it goes in unit instead and does enter the key.\n\n\n\n\n\n","title":"Dendro.Location"},{"category":"type","location":"api/#Dendro.Rule","page":"API reference","text":"Rule\n\nOne lint rule. name is the metric a finding reports under and the name an inline dendro-ignore directive accepts. kind is :scalar or :flag. A scalar rule carries its (warn, high) band; a flag rule carries nothing. fn measures one unit or the whole index:\n\nscalar fn(unit, index) -> Int, scored against the band and the corpus percentile.\nflag fn(index) -> Vector{TreeSitter.Node}, one finding per returned node.\n\nscope says which units a scalar rule measures: :any, or :callable for a rule that asks about a definition. function_length measures the distance to a boundary an author drew and parameter_count reads a signature, and top-level code has neither, so a band calibrated on definitions must not be read there.\n\nseverity is the absolute band a flag rule's findings carry, and is meaningful only for a flag: a scalar reads its band instead. Every built-in flag is :high, which is what puts it in the errors floor. A user-authored pattern rule defaults to :warn, since a rule firing across a corpus would otherwise make the gate unsatisfiable. The two kind-specific fields mirror each other: band is scalar-only, severity flag-only.\n\nfn is abstractly typed, so calling it dispatches dynamically. Parametrising it would make every rule a distinct type, and the rule set is a Vector{Rule} a project extends at run time, which needs one. The dispatch is paid once per unit per rule, never per node.\n\n\n\n\n\n","title":"Dendro.Rule"},{"category":"constant","location":"api/#Dendro.BUILTIN_RULES","page":"API reference","text":"BUILTIN_RULES :: Vector{Rule}\n\nThe default rule set, in report order. Scalar bands are fixed (warn, high) targets, so a uniformly-weak codebase has a standard to improve toward rather than only its own median. Drawn from common complexity guidance. Pass rules to analyze to extend or replace them.\n\n\n\n\n\n","title":"Dendro.BUILTIN_RULES"},{"category":"constant","location":"api/#Dendro.OPTIONAL_RULES","page":"API reference","text":"OPTIONAL_RULES :: Vector{Rule}\n\nRules a caller can opt into but that are off by default: return_count needs per-project band tuning, trivial_wrapper has a higher false-positive rate, unreachable_after_jump flags code after an unconditional jump, npath grows multiplicatively so its band wants per-project tuning, local_count likewise, shadowed_variable reads name collisions some idioms make routine (a method local matching a class attribute), and fan_out cannot separate a smell from a legitimate orchestrator by any fixed band (idiomatic corpora run p99 from 9 to 26 distinct callees). Use them with analyze(path; rules = [BUILTIN_RULES; OPTIONAL_RULES]).\n\n\n\n\n\n","title":"Dendro.OPTIONAL_RULES"},{"category":"type","location":"api/#Dendro.PatternSpec","page":"API reference","text":"PatternSpec\n\nOne user-authored lint rule, as declared by a [patterns.<name>] table in a .dendro.toml. Where a Rule carries the function that measures, a spec carries what a project said it wants; pattern_rule turns one into the other once the query behind it has compiled.\n\nname is the metric a finding reports under and the name an inline dendro-ignore accepts. message is what the rule says when it fires. severity is :warn or :high, deciding whether the rule reaches the errors floor. kind is :flag or :scalar; a scalar carries its (warn, high) band and a flag carries nothing.\n\nscope is the unit kind a scalar rule measures, :any or :callable, the same declaration a built-in Rule carries. A rule counting a shape \"in one function\" means the definitions, and top-level code is not one.\n\nguard says the rule is written to catch something the project intends never to write, so matching nothing is the state it wants rather than a rule to go and fix. Only the zero-match report reads it, and everything else treats a guard as any other rule. Without it that report, which exists to catch a query naming a shape the grammar never produces, cannot tell that case from a rule doing its job.\n\nThe declaration is language-independent. One <lang>.patterns.scm per grammar realises it, so a rule says one thing across every language a project scans.\n\n\n\n\n\n","title":"Dendro.PatternSpec"},{"category":"function","location":"api/#Dendro.check_patterns","page":"API reference","text":"check_patterns(paths; config=nothing) -> Vector{PatternTestFailure}\n\nRun every declared pattern rule against the fixtures in its pattern directories and report where a fixture and the rule disagree.\n\nFixtures live in a tests/ folder beside the queries, one file per language, and mark the lines a rule must match with a dendro-expect: <rule> comment. A marked line the rule did not match is a miss; an unmarked line it did match is a false positive. Both fail.\n\nWhat a fixture pins is what the query matched, for either kind of rule. A scalar rule's band and percentile are Dendro's own scoring and are tested elsewhere, so a scalar fixture marks the lines its occurrences sit on exactly as a flag fixture does.\n\nA rule with no fixture is not a failure. Requiring one would be friction on writing a two-line house rule, and the zero-match report already catches the rule that never fires at all.\n\n\n\n\n\n","title":"Dendro.check_patterns"},{"category":"type","location":"api/#Dendro.PatternTestFailure","page":"API reference","text":"PatternTestFailure\n\nOne disagreement between a fixture and what a rule did: file and line locate it, rule names the rule, and kind is :missed (a marked line the rule did not report) or :unexpected (an unmarked line it did).\n\n\n\n\n\n","title":"Dendro.PatternTestFailure"},{"category":"type","location":"api/#Dendro.Config","page":"API reference","text":"Config\n\nResolved tuning thresholds for one analysis, built by discover_config from the built-in defaults and a .dendro.toml. cut is the percentile cutoff; bands overrides scalar rule (warn, high) tuples by metric name; one field per relational metric overrides that metric's band; rules toggles a rule on or off by name, and the reimplementation, incoherent_package and divisible_package corpus passes with it; min_size, threshold, and radius_factor are the clone-detection thresholds; reimpl_threshold is the reimplementation overlap cutoff; languages carries the languages the config registers beyond the ones Dendro ships, each a LanguageProfile naming where to load its grammar and queries from; and patterns carries the user-authored lint rules the config declares, each a PatternSpec, sorted by name so a report reads in a stable order, and patterns_dir overrides where their queries are read from (empty for the default, .dendro/patterns beside the config); library_threshold and library_gate_coverage are the cross-corpus duplication thresholds and library_anchor_grain widens the near pass to compare blocks as well as whole functions; libraries holds the reference corpora a [libraries.<name>] table declares, each a Library, sorted by name; and ignore holds gitignore-style patterns dropping paths from the scan, which analyze's own ignore keyword adds to rather than replaces. Immutable: pass one to analyze with config = to skip file discovery.\n\n\n\n\n\n","title":"Dendro.Config"},{"category":"type","location":"api/#Dendro.Library","page":"API reference","text":"Library(roots; ignore = String[])\nLibrary(name, roots; ignore = String[])\n\nA reference corpus: source Dendro reads to compare the project against, and never scores. roots is one directory or a list of them, ignore a list of gitignore-style patterns as analyze's own ignore. Pass a Vector{Library} as analyze's libraries to report :library_duplicate and :library_near_duplicate findings at the sites in the project that a library already implements.\n\nLibrary(\"Base\", \"/usr/local/julia/base\"; ignore = [\"precompile.jl\"])\nLibrary(\"MyDep\", [\"dep/src\", \"dep/ext\"])\nLibrary(\"~/.julia/packages/IterTools/A1b2C/src\")     # name derived\n\nAbsent a name, the first root's basename is used, falling back to the parent's when that basename is a conventional source directory, so dep/src reads as dep. The name is display only: nothing keys on it, and a version slug in a depot path is not worth guessing at, so name a library in the config when the path does not say it.\n\nRoots are expanded and made absolute on construction, and a root that is not a directory throws.\n\n\n\n\n\n","title":"Dendro.Library"},{"category":"section","location":"gating/#Gating-CI","page":"Gating CI","text":"errors is the gate companion to analyze. Where analyze ranks by percentile for triage and so is never empty, errors returns only the error-severity findings, the :high-band floor, so it is satisfiable: a clean codebase returns nothing. Assert it in your test suite and every Pkg.test() run gates on Dendro.\n\n@testitem \"Dendro quality gate\" begin\n    using Dendro\n    errs = Dendro.errors(\"src\"; since = get(ENV, \"DENDRO_BASE\", nothing))\n    isempty(errs) || show(stdout, MIME\"text/plain\"(), errs)   # name the findings in the CI log\n    @test isempty(errs)\nend\n\nshow prints the per-finding report above the assertion, so a failing gate names the functions that tripped it instead of just Evaluated: false.\n\nThe floor is every finding at the :high absolute band: high-band scalars and all flags, percentile-free. Nothing in it depends on the corpus distribution, so the same code gates the same way whatever else it is scanned alongside.","title":"Gating CI"},{"category":"section","location":"gating/#The-ratchet","page":"Gating CI","text":"since turns the floor into a ratchet: the findings at the working tree minus those at a base git ref, the answer to \"did this change introduce a violation\". A finding that predates the ref, even on a line the change touched, is not reported, which is what makes Dendro adoptable on a codebase that is not yet clean.\n\nSet DENDRO_BASE in CI to the pull request's base (origin/main, or the merge base) and leave it unset locally, where errors falls back to the absolute floor.\n\nFrom a shell the same gate is dendro --check --since=origin/main src, described under Command line.\n\nsince is distinct from analyze's base. base is spatial, scoping annotations to changed lines. since is a finding-set difference, the gate.","title":"The ratchet"},{"category":"section","location":"gating/#Pull-request-annotations","page":"Gating CI","text":"github_annotations emits the same findings as GitHub Actions workflow commands. GitHub records each as a pull-request check annotation, high-band findings as ::error, the rest as ::warning:\n\n::error file=src/parser.jl,line=1070,title=Dendro%3A cyclomatic::predicate: cyclomatic 51 (high; p100)\n\nPair this with base to scope findings to the functions a change touched. An annotation renders inline on the diff when its anchored line falls in the change; otherwise it shows in the run's Checks tab.\n\nanalyze loads each language's parser from the active environment, so the workflow adds the tree_sitter_<lang>_jll for the languages it analyses. See .github/workflows/dendro.yml for a working setup.","title":"Pull-request annotations"},{"category":"section","location":"diagrams/#Diagrams","page":"Diagrams","text":"To see the structure rather than read it, mermaid renders one of the graphs Dendro builds as a mermaid flowchart. graph picks which: :coupling for the reference graph behind :misplaced and :scattered, :reachability for the graph behind :unreferenced, :clones for the clone clusters, and :change for what a change did to the file graph.\n\nusing Dendro: mermaid\n\nmermaid(\"src\"; graph = :coupling, granularity = :file)   # module-coupling map to stdout\nopen(io -> mermaid(io, \"src\"; graph = :reachability), \"dead.mmd\", \"w\")\n\nA :unit graph of a real corpus is a hairball, one node per function, too dense to read and too large for the standard mermaid renderer, so focus trims it to what the findings touch. The default resolves that per granularity, and focus = :all opts back into the full graph. context sets how far a trimmed view reaches around each flagged node: 0 keeps the flagged nodes alone, 1 their immediate neighbours. A :clones graph is already finding-only, so neither keyword changes it.","title":"Diagrams"},{"category":"section","location":"diagrams/#What-a-change-did-to-the-shape","page":"Diagrams","text":"The other diagrams draw one revision. graph = :change draws two, the file graph at a base git ref against the file graph at the working tree, and keeps only the edges whose weight moved.\n\nmermaid(\"src\"; graph = :change, base = \"main\")\n\nThis answers the question a reviewer has before \"is this code bad\": did the edit stay in one place, or did it rewire the corpus. The scalar rules already report a function that got worse, and the architecture rules report a directory pair or a file that crossed a band. Neither says anything about a change that adds thirty references without pushing any single thing past a threshold, and that change may still have moved the shape.\n\nThe state of each edge is in the arrow, so the diagram survives being read without colour:\n\narrow meaning label\n==> added, or carrying more references than before new: <names>, or +N <names>\n-.-> fewer references than before, or gone -N, or gone\n\nThe label names the definitions the edge gained. \"a.jl now depends on b.jl\" is not something to act on; \"a.jl now reaches parse_config in b.jl\" is. An edge that lost references names nothing, since those definitions are gone from the working tree.\n\nReading one, from a change that extracted scoring.jl out of report.jl: ten rule files each grew an edge into the new file and thinned their edge into the old one by about as much. That paired gain and loss is what an extraction looks like, and one shape stands in for ten hunks of diff. An edge nobody expected, or a pair of files that started pointing at each other, is the thing to ask about.\n\n:change reads git, so paths must sit inside a repository and base must name a commit. A change that rewired nothing draws a header and no edges, which is the honest answer rather than an empty file.","title":"What a change did to the shape"},{"category":"section","location":"diagrams/#Inside-one-file","page":"Diagrams","text":"A file edge is a reference that crossed a file boundary, so an edit that stays inside one file draws nothing at all. Splitting a 200-line function into five moves plenty of structure and moves no file edge. granularity = :unit reads the level below: each file's own references, one subgraph per file, the same arrows.\n\nAn arrow runs from the unit that named a definition to the unit holding it, and its weight is how many times it named it, so a call added inside a file thickens one arrow and says which two units it joined. A definition at file scope that the file's functions read, a constant or a lookup table, is a node of its own rather than folded into whichever function sits beside it.\n\nmermaid(\"src\"; graph = :change, base = \"main\", granularity = :unit)\n\nUnits are matched across the two revisions by name, and then by the body digest the clone passes already compute, so a function renamed with its body intact is one node labelled new (was old) and not a deletion beside an addition. Two identical stubs renamed at once are ambiguous, and Dendro will not guess: both report as a deletion and an addition. Overloads merge into one node per name, since a method's position in a file moves for reasons a reviewer does not care about.\n\nFiles are drawn heaviest mover first, and a file whose units never moved is absent rather than drawn empty.\n\nnote: This view is not yet gated\nEvery file with a moved edge is drawn, which on a large change is more diagram than anyone reads. Deciding which files earn a picture, and collapsing the rest behind a one-line summary, waits on where the output is delivered. Until then, prefer it on a focused diff or a single directory.","title":"Inside one file"},{"category":"function","location":"diagrams/#Dendro.mermaid","page":"Diagrams","text":"mermaid(io, paths; graph=:coupling, granularity=:file, focus=:auto, context=1, ...)\nmermaid(paths; ...) -> nothing\n\nRender one of Dendro's graphs over paths as a mermaid flowchart, written to io. graph selects the diagram: :coupling the corpus reference graph behind :misplaced and :scattered, :reachability the dead-code graph behind :unreferenced, :clones the duplicate clusters, :change the file graph at a base git ref against the file graph at the working tree. granularity is :file (units collapsed to their file) or :unit (one node per function). Active findings overlay onto the diagram: misplaced and scattered nodes are classed, the misplaced suggested home drawn as a dashed edge, dead definitions and public roots classed.\n\n:change answers a reviewer's first question on a large diff, whether an edit stayed in one place or rewired the corpus. Only the edges whose weight moved are drawn, with the state in the arrow: ==> for an edge added or grown and -.-> for one weakened or gone. At :file an edge is the references crossing from one file to another, and the arrow is labelled with the definition names it gained. At :unit it is one file's own references, drawn from the unit that named a definition to the unit holding it, so an edit that never left a file still shows; the node it points at already carries the definition's name, so the arrow carries only the weight. A unit is matched across the revisions by name, and by body digest where the name moved, so a rename reads as one node with a history rather than a deletion and an addition. It needs base, and a change that rewired nothing draws a header and no edges. Unlike the other diagrams it reads git, so paths must sit inside a repository and base must name a commit.\n\nfocus trims the diagram to what the findings touch. :findings keeps only flagged nodes and the context hops of graph neighbours around them, drawn greyed, so a unit-level graph of a real corpus stays small enough to read and to render. :all keeps every node. :auto (the default) resolves to :findings at :unit granularity, where the full graph is a hairball, and :all at :file, where it is already legible. context is the neighbour radius: 0 keeps only the flagged nodes, 1 their immediate neighbours. :clones is already finding-only, so focus does not change it.\n\nUnlike github_annotations, which renders Findings, this takes the corpus and rebuilds the structure it draws, since a graph is not recoverable from findings. Redirect io to a .mmd file to save the diagram, as the CI workflow does for annotations. The keyword options match analyze's clone and ignore tuning.\n\nprofiles is the language registry the corpus resolves file extensions through. It defaults to the languages Dendro ships; pass Dendro.resolve_profiles(config) to draw a graph over a language a .dendro.toml registers, which analyze does from its own config.\n\n\n\n\n\n","title":"Dendro.mermaid"},{"category":"section","location":"suppression/#Suppressing-findings","page":"Suppressing findings","text":"Some flagged code is fine in context. A comment directive accepts a specific finding so Dendro skips it without muting the tool or refactoring sound code. The mechanism reads comment nodes, so it works in every supported language.\n\ndendro-ignore suppresses every finding on the same line or the line directly below, so a trailing comment or a comment above a declaration both work.\ndendro-ignore: cyclomatic, parameter_count suppresses only the named metrics.\ndendro-ignore-file (or dendro-ignore-file: cyclomatic) suppresses the whole file, for generated or vendored code.\nA directive opens the comment it sits in: only comment punctuation and whitespace may precede it. A comment discussing the mechanism is therefore not one, so a project can document its own suppressions without exempting the code underneath.\n\n# dendro-ignore: parameter_count\nfunction build(a, b, c, d, e, f)   # one keyword per field, accepted\n    ...\nend\n\nA directive names the active rules and the relational metrics, listed in Metric reference; a custom rule's name is accepted too. An unknown name warns, so a typo does not silently disable a check. dendro-ignore-file: low_cohesion is the usual way to accept a file that is meant to be a grab-bag.\n\nSuppression marks a finding rather than dropping it. Printing a findings vector lists the active findings and a footer counting the suppressed ones, and active returns only the unsuppressed findings for gating.","title":"Suppressing findings"},{"category":"section","location":"suppression/#Ignoring-paths","page":"Suppressing findings","text":"dendro-ignore-file mutes one file from inside it. Vendored and generated trees you do not own want the opposite: exclusion from the outside, by path, without touching the source. The ignore keyword takes gitignore-style patterns, matched against each path relative to the scanned folder.\n\nanalyze(\".\"; ignore = [\"vendor/\", \"deps/**\", \"*.generated.jl\"])\n\nThe same patterns go in a .dendro.toml, which is the route a command-line scan has:\n\nignore = [\"vendor/\", \"deps/**\", \"*.generated.jl\"]\n\nThe keyword adds to what the file declares, so a caller narrows a scan further without restating the project's own exclusions.\n\nA leading ! re-includes, a trailing / matches directories only, * and ? stop at a separator, ** spans them. As in gitignore, a file under an excluded directory cannot be re-included.\n\nIgnored files are dropped before parsing, so they are neither flagged nor counted in the baseline. This matters even in base review mode: an unchanged vendored tree never appears in findings, but left in the corpus it would still skew the percentile every scanned file feeds. Ignoring it keeps relative scoring honest. Patterns apply to folder scans, not a single named file.","title":"Ignoring paths"},{"category":"section","location":"configuration/#Configuration-file","page":"Configuration file","text":"The bands a finding is judged against are drawn from common complexity guidance. They are deliberate opinions, and a project retunes them from a .dendro.toml at its repo root, with no code changes.\n\nOnly the flagging opinions are configurable: the bands, the percentile cut, the clone thresholds, which rules are active, which libraries a scan compares against, which languages are registered, and which paths are left out of the scan. The corpus floors and the model internals are not.","title":"Configuration file"},{"category":"section","location":"configuration/#The-cascade","page":"Configuration file","text":"Discovery is a cascade, merged key by key, last wins:\n\nthe built-in defaults,\na user-global ~/.config/dendro/config.toml (XDG_CONFIG_HOME if set),\nthe repo .dendro.toml, found at the git toplevel of the scanned roots,\nany explicit analyze keyword.\n\n--config=<file> reads one file in place of discovery and --no-config ignores config files entirely; both are described in Command line.\n\nThe repo file is found once per run, not per subtree. One corpus means one baseline and one set of bands, since the corpus-relative score is global and per-directory bands would be incoherent with it.","title":"The cascade"},{"category":"section","location":"configuration/#Bands,-the-cut,-and-which-rules-run","page":"Configuration file","text":"# .dendro.toml\ncut = 0.97                 # percentile cutoff for corpus-relative flags\n\n[bands]\ncyclomatic = [15, 30]      # scalar metric: override (warn, high)\nfunction_length = [60, 120]\nlow_cohesion = [5, 7]      # relational metric: override its band\nback_edge = [90, 97]\ndependency_cycle = [6, 12]\n\n[rules]\nnpath = true               # enable an optional rule\nparameter_count = false    # disable a built-in rule\n\n[bands] keys are the scalar metric names plus the relational metrics that carry a band, each taking a [warn, high] pair. Metric reference lists which names those are and what each defaults to.\n\n[rules] keys are any rule name, on or off. The opt-in passes are switched on here too: reimplementation, incoherent_package, divisible_package, distant_definition, library_duplicate and library_near_duplicate.","title":"Bands, the cut, and which rules run"},{"category":"section","location":"configuration/#Clone-detection","page":"Configuration file","text":"[clones]\nmin_size = 12          # min named-node subtree to count as a clone\nthreshold = 0.9        # near-miss similarity cutoff\nradius_factor = 0.5    # candidate-search radius, as a fraction of function size\n\nThe three cross-corpus keys live in the same table, since they are clone thresholds too: library_threshold, library_gate_coverage, and library_anchor_grain, documented with the values they were measured at under Duplication against a library.","title":"Clone detection"},{"category":"section","location":"configuration/#Tables-owned-by-a-feature","page":"Configuration file","text":"Four tables are the configuration surface of one feature each, and each is documented where that feature is:\n\nTable Declares Read\n[libraries.<name>] a reference corpus, by path or paths Comparing against a library\n[reimplementation] the vocabulary-overlap cutoff Duplicate detection\n[languages.<name>] a language to register, or a shipped query to replace Adding a language\n[patterns.<name>] a project's own rule, realised by a query under patterns_dir Pattern rules\n\nTwo top-level keys those leave here. patterns_dir points at the directory holding the pattern queries, resolved against the config file that set it, and defaults to .dendro/patterns beside the config. ignore takes gitignore-style patterns that drop paths before parsing, so a vendored or generated tree is neither flagged nor counted in the baseline:\n\nignore = [\"vendor/\", \"deps/**\", \"*.generated.jl\"]\n\nSuppressing findings covers the pattern syntax and why an excluded tree has to leave the baseline too. analyze's ignore keyword adds to this list rather than replacing it.","title":"Tables owned by a feature"},{"category":"section","location":"configuration/#When-a-key-is-wrong","page":"Configuration file","text":"An unknown key warns and is ignored, so a typo is visible rather than silent and a file written for a newer Dendro still applies the keys this version knows.\n\nA library path matching nothing is the one exception and errors instead: a library resolving to nothing would silently turn its gate off, which is the failure that feature exists to prevent.","title":"When a key is wrong"},{"category":"section","location":"metrics/#Scoring-and-metrics","page":"Scoring and metrics","text":"","title":"Scoring and metrics"},{"category":"section","location":"metrics/#Scoring","page":"Scoring and metrics","text":"Every scalar metric reports two scores, and a function is flagged when either fires:\n\nAbsolute: the value against a fixed band (cyclomatic warn >10 / high >20, nesting >3, parameters >4). A fixed target a codebase can improve toward.\nRelative: the value's percentile against the corpus. Catches functions worse than the codebase's own norm, the signal that matters in review.\n\nAbsolute alone misses outliers in a uniformly-weak codebase; relative alone calls a uniformly-weak codebase fine. Reporting both avoids each trap.","title":"Scoring"},{"category":"section","location":"metrics/#Metrics","page":"Scoring and metrics","text":"Metric reference tabulates every name below with its default band and whether it runs by default. This section says what they measure.\n\nScalar (per function): cyclomatic complexity, cognitive complexity (the same branch points weighted by the nesting they sit under, so a deeply-nested function scores worse than a flat one of the same path count), length, maximum nesting depth, parameter count, boolean complexity (the most &&/|| operators joined into one expression).\n\nFlag (presence is the finding): swallowed errors (empty catch clauses), stub markers (TODO/FIXME/XXX/HACK comments), empty function bodies, a return inside a finally clause (which discards a pending error or return value), identical operands (x == x, a && a), a conditional whose branches are all identical (if c then X else X), unused parameters, unused locals, and broad catches (a bare except:, except BaseException, Java catch (Throwable), C++ catch (...), Ruby rescue Exception, PHP catch (Throwable), the handlers that swallow interrupts and exits; the merely-wide except Exception tier is left alone). The optional unreachable_after_jump flags code after an unconditional return, break, or throw.\n\nUnused parameters and locals read the lexical bindings: a parameter or local binding whose name nothing in its function references is dead weight. The use-test is by name over the whole unit, the conservative reading, so a same-named reference in a nested closure counts as a use. A leading underscore opts a name out, the cross-language deliberate-unused convention. A bodyless declaration keeps its parameters (they are its signature), an empty or stub body is already the empty_body finding, and a top-level binding belongs to :unreferenced, so none of those double-report. A language whose parameters carry no names (bash) or whose scopes query captures no locals (php) reports nothing for that half.\n\nThree more coupling and binding readings are optional rules: local_count (distinct local names bound in a function), shadowed_variable (a fresh local binding hiding an enclosing one), and fan_out (distinct callables a function invokes). See Custom rules.\n\nEach metric is a rule. The set above is the default; a caller can add their own or opt into rules that are off by default.\n\nRelational (computed across the corpus, not per function): duplicates and opt-in reimplementation candidates (Duplicate detection), naturalness, within-file cohesion, and cross-file placement. Naturalness scores each function's token sequence against a per-language trigram model of the rest of the corpus, in bits per token. The corpus model is interpolated with a per-file cache model (after Tu et al., \"On the Localness of Software\"), so a function is read against its own file's idiom, not just the corpus's, which sharpens genuine outliers and quiets file-consistent patterns. A surprising, unidiomatic function scores high, and surprise correlates with bugs. Reported as :unnatural with both scores, the absolute cross-entropy band and the corpus percentile. A language with too few tokens to model is skipped.\n\nCohesion asks whether a file's functions group by usage, placement whether a unit sits in the right file, scattering whether a file's units belong to one module, and reachability whether a private definition is dead, reported as :unreferenced. All four are in Cohesion and placement. The readings taken one level up, over files depending on files, are in Dependencies and layout.","title":"Metrics"},{"category":"section","location":"languages/#Languages-and-limitations","page":"Languages and limitations","text":"","title":"Languages and limitations"},{"category":"section","location":"languages/#Languages","page":"Languages and limitations","text":"bash, c, cpp, go, java, javascript, julia, php, python, ruby, rust, typescript.\n\nJSON and HTML are out of scope: with no functions or control flow, these metrics do not apply.","title":"Languages"},{"category":"section","location":"languages/#What-cross-file-resolution-reaches-per-language","page":"Languages and limitations","text":"Every rule past a single file rests on matching a reference to the definition it names along declared linkage. How far that reaches differs by language, and where it reaches nothing the rules go quiet rather than reporting a problem. A quiet report is not the same as a clean one, so it is worth knowing which case you are in.\n\nJulia, Python, C, Go and Rust resolve normally: a reference names a top-level function, so both the unit graph and the file graph fill in and every rule applies.\n\nJava and PHP resolve types, not methods. A method is reached through a receiver whose type the resolver never works out, so what one Java or PHP file sees of another is the class name. The file graph is unaffected and well populated, 1220 edges across Guava's collect package and 304 across Laravel's Database, so :back_edge, :dependency_cycle, :hub and :split_audience all work, and so does :divisible_package. The unit graph carries no cross-file edge at all, because a class definition is not a function unit, which leaves :misplaced, :scattered and :incoherent_package silent on a Java or PHP corpus. That is the type-and-dispatch line holding, not a gap to close: following a method call to its definition is exactly the resolution Dendro does not do.\n\nJavaScript resolves ES modules only. The linkage query reads import ... from and export. A corpus written in CommonJS, require() and module.exports, resolves nothing across the boundary, so every cross-file rule is silent on it. Express and Lodash are both in this position.\n\nRuby resolves require_relative only. A plain require names a file through a load path the source does not carry, and a method inside a module or class body is not spliced into the requiring file's namespace in any case, so a conventionally-written Ruby library resolves nothing. Sinatra is in this position.\n\nTypeScript resolves its ESM output convention, where a specifier names the emitted ./a.js and the source on disk is a.ts.","title":"What cross-file resolution reaches per language"},{"category":"section","location":"languages/#Limitations","page":"Languages and limitations","text":"Ruby swallowed-rescue is not flagged. Its handler body is inline rather than a block, so it does not fit the detection model.\nSwitch default adds one to complexity in C, C++, and Java (default shares the case node) but not in Go, JavaScript, TypeScript, PHP, or Ruby (default has its own node).\nGo empty-body detection is weak: a Go function body always wraps a statement list, so empty bodies do not register.\nMetrics are syntactic. Dendro resolves names lexically, within a file and across declared include/import/export edges, but never types or dispatch. Concerns that need type or dispatch resolution (overload resolution, real call graphs, dead code across files) are out of scope.\nCross-file placement sees only the linkage a language ships a query for, and only the include/import edges present in the scanned corpus. A name matching several visible definitions is resolved by name, not dispatch, so its weight is split across them. Dynamic imports and re-exports are not followed.","title":"Limitations"},{"category":"section","location":"patterns/#Pattern-rules","page":"Pattern rules","text":"A Rule measures the concept vocabulary a language's <lang>.scm query produces: decision points, nesting, catch clauses, parameters. That vocabulary is closed on purpose, since it is what keeps metric code free of grammar node types. It also means a rule cannot name a shape Dendro has no concept for.\n\nA pattern rule can. It is a second query family, <lang>.patterns.scm, where a capture names a rule instead of a concept. The set of names is open and the queries are written against concrete grammar node types deliberately, because being language-specific is the point.","title":"Pattern rules"},{"category":"section","location":"patterns/#Declaring-a-rule","page":"Pattern rules","text":"A rule is declared once, language-independently, in .dendro.toml:\n\n[patterns.empty_catch_binding]\nmessage  = \"`catch` with no exception binding discards the error\"\nseverity = \"high\"\n\n[patterns.magic_number]\nmessage = \"unnamed numeric literals in one function\"\nkind    = \"scalar\"\nband    = [5, 10]\n\nkey \nmessage required; what the rule says when it fires\nseverity \"warn\" (default) or \"high\", for a flag rule\nkind \"flag\" (default) or \"scalar\"\nband required by a scalar, rejected on a flag; must start at 1 or higher\nguard false (default); true when matching nothing is the wanted state\n\nseverity defaults to warn because errors gates on the :high band. A rule that fires across a corpus would otherwise make the gate unsatisfiable the day it is written. Promoting one to high is a line of config and the project's call.\n\nA scalar's band starts at 1 or higher so that a unit with no matches scores :ok. A band starting at zero would report every function in the corpus for containing nothing.\n\nMetadata lives here and never beside the queries. A rule name is language-independent and a query is not, so a message kept next to a query would be either per language and free to drift, or in a second config file with a second cascade to merge. A pattern rule also toggles off through [rules] by the same name a built-in does:\n\n[rules]\nmagic_number = false","title":"Declaring a rule"},{"category":"section","location":"patterns/#Writing-the-query","page":"Pattern rules","text":"Queries live in .dendro/patterns/<lang>.patterns.scm beside the config, and in ~/.config/dendro/patterns/ for rules shared across repositories. Both locations are read and compose; where both define the same rule for the same language, the repo's wins for that pair.\n\nA capture names the rule a match reports under, and Pattern query syntax is what a query file may say: the capture conventions, the predicates available, and how a repeated shape factors out.","title":"Writing the query"},{"category":"section","location":"patterns/#One-rule-in-two-grammars","page":"Pattern rules","text":"A rule declared once and realised twice is what the split between the declaration and the queries is for. optional_equality catches x == nothing in Julia and x == None in Python, and the two queries look nothing alike:\n\n; julia.patterns.scm\n((binary_expression (operator) @_op (identifier) @_n) @optional_equality\n (#eq? @_op \"==\") (#eq? @_n \"nothing\"))\n((binary_expression (identifier) @_n (operator) @_op) @optional_equality\n (#eq? @_op \"==\") (#eq? @_n \"nothing\"))\n\n; python.patterns.scm\n((comparison_operator \"==\" (none)) @optional_equality)\n\nPython keeps the operator in comparison_operator as an anonymous token, so one pattern covers both operand orders. Julia makes it a named child, and sibling patterns match in source order, so each order needs its own pattern. The message the finding carries is declared once for both.\n\nWhere a query cannot resolve a name it reads convention instead, and both languages need the same carve-out for it: typeof(x) == typeof(y) and type(x) == type(y) compare two types that arrived as values, which == answers correctly. A capitalised right operand is what separates those from a comparison against a type written down.\n\nFixtures are where a query's disagreements with a grammar get recorded, and a rule written against an operator has one waiting: x!==nothing without spaces parses as the identifier x! and the operator ==, so correct Julia reads as the shape the rule looks for. A .not pattern reading the left operand is what excludes it, and a fixture line is what keeps the exclusion from being deleted later as dead weight.","title":"One rule in two grammars"},{"category":"section","location":"patterns/#Flag-rules-and-scalar-rules","page":"Pattern rules","text":"A flag rule reports one finding per matched node, carrying its declared severity as the absolute band. A scalar rule counts its matches per unit and is scored against its band and the corpus percentile, exactly as cyclomatic is, including retuning through [bands]:\n\n[bands]\nmagic_number = [3, 6]\n\nA scalar counts through the same fold every built-in scalar uses, so it stops at nested callables: a closure's matches belong to the closure, not to the function around it.\n\nBecause a pattern rule resolves to an ordinary Rule, everything else follows with no special handling. Findings carry dendro-ignore suppression by rule name, scope to a diff under analyze's base, reach errors when their band is :high, and participate in the ratchet under errors(paths; since).","title":"Flag rules and scalar rules"},{"category":"section","location":"patterns/#When-a-rule-is-wrong,-it-says-so","page":"Pattern rules","text":"A rule that reports nothing reads as clean code, which is the failure this design works hardest to prevent. Every way of getting a rule wrong is loud.\n\nTree-sitter validates a query when it compiles, so a node type the grammar does not have, an unknown field, a capture reference no pattern binds, and a syntax error all fail at load, naming the file and the line:\n\ndendro: node type error at line 3 of .dendro/patterns/julia.patterns.scm\n\nA predicate TreeSitter.jl does not implement compiles cleanly and then rejects every match, so it is rejected at load instead, with the alternatives named. A capture naming no declared rule is an error, and so is a fragment sharing a rule's name. An error inside an expanded fragment names the fragment, where it was defined, and where it was used, rather than an offset into text nobody wrote.\n\nThe one case none of that reaches is a well-formed query naming a shape that simply never occurs. A rule that matched nothing anywhere in the corpus is reported after the scan:\n\nwarning: pattern rule(s) matched nothing: no_such_shape","title":"When a rule is wrong, it says so"},{"category":"section","location":"patterns/#Guard-rules","page":"Pattern rules","text":"Most rules worth writing are silent on a codebase that is already clean, and that silence is the result they were written for. It reads identically to the broken rule above, so the declaration is what tells them apart:\n\n[patterns.nothing_equality]\nmessage = \"`== nothing` compares where `=== nothing` identifies\"\nguard   = true\n\nA guard is never reported as unmatched. It is not switched off: write the shape and the finding arrives as it would from any other rule. Reach for it when the rule names something the project has decided never to write, and leave it off when the rule is meant to find something and you want to hear that it did not.","title":"Guard rules"},{"category":"section","location":"patterns/#Testing-a-rule","page":"Pattern rules","text":"Those catch a rule that is malformed, not one that fires on the wrong thing. Fixtures pin both directions. They live in a tests/ folder beside the queries, one file per language, and mark the lines a rule must match:\n\n# .dendro/patterns/tests/julia.jl\ntry r() catch    # dendro-expect: empty_catch_binding\nend\ntry r() catch e  # unmarked, so the rule must not fire here\nend\n\n$ dendro --check-patterns src/\n\nA marked line the rule missed is a failure, and so is an unmarked line it matched. Checking for false positives is the point: a check that only asked whether a rule matched something would pass a rule that matched everything.\n\nA fixture records what the query matched, for either kind of rule. A scalar rule's band and percentile are Dendro's own scoring and are tested elsewhere, so a scalar fixture marks the lines its occurrences sit on exactly as a flag fixture does.\n\nA rule with no fixture is not a failure. Requiring one would be friction on writing a two-line house rule, and the zero-match report already covers the rule that never fires.\n\ncheck_patterns is the same check from Julia, returning a PatternTestFailure per disagreement, so a project can assert on it in its own suite.","title":"Testing a rule"},{"category":"section","location":"patterns/#What-a-pattern-rule-cannot-do","page":"Pattern rules","text":"A query reads a node's shape, its text, and its ancestry. It cannot see bindings and it cannot resolve a type, which is the same line every other part of Dendro holds.\n\nA rule needing either stays a Julia Rule passed to analyze's rules, described under Custom rules. That path is reachable from Julia but never from a config file: loading Julia named by a repo's .dendro.toml would execute it during a scan.\n\nMeasured against the default rule sets of Clippy, Ruff, and ESLint, roughly half of a sampled 150 rules are expressible as pattern rules. The remainder needs types or trait resolution (about a sixth), is formatting and node ordering (about a quarter), needs binding resolution (a twentieth), or is something Dendro already reports. The fit is best for Python, at around 70%, and weakest for Rust, at 44%, where linting resolves traits.","title":"What a pattern rule cannot do"},{"category":"section","location":"#Dendro.jl","page":"Home","text":"Code-maintainability metrics on tree-sitter syntax trees, a cheap automatic gate for generated code. Dendro walks the parse tree of a file, computes per-function metrics, and scores each function two ways: against fixed absolute bands and against the codebase's own distribution. Point it at a git diff and it scores only the functions a change touched.\n\nDendro never builds or runs the code. It reads syntax, so it works on a half-finished branch, on a file that does not compile, and across a dozen languages from one set of metric definitions. Every metric is a structural approximation: complexity by counting branch nodes, not by analysing control flow. Cheap and broad over precise and deep.\n\nBuilt on TreeSitter.jl. Parsers load lazily, so Dendro depends on no language grammars itself. Add the tree_sitter_<lang>_jll for the languages you analyse.","title":"Dendro.jl"},{"category":"section","location":"#Install","page":"Home","text":"import Pkg\nPkg.add(url = \"https://github.com/MichaelHatherly/Dendro.jl\")\n# plus the grammars you want to analyse, e.g.\nPkg.add([\"tree_sitter_julia_jll\", \"tree_sitter_python_jll\"])","title":"Install"},{"category":"section","location":"#Usage","page":"Home","text":"analyze is the one entrypoint. Point it at a folder or a file.\n\nDendro exports nothing; its API is marked public. Import what you call, or qualify with Dendro..\n\nusing Dendro: analyze, active\n\n# Analyse a whole project. Each function is scored against the corpus's own\n# distribution, and duplicates across files are reported. The baseline is built\n# from the corpus, so relative scoring works with no setup. The returned vector\n# prints as a report in the REPL.\nanalyze(\"src\")\n\n# Review mode: only the functions a change touched, scored against the\n# full-corpus baseline.\nanalyze(\"src\"; base = \"HEAD\")\n\n# Analyse one file. Language is inferred from the extension; the file's own\n# functions are the corpus it is scored against.\nanalyze(\"src/parser.jl\")\n\n# Scan several roots as one corpus. A package's `src` and `ext` are measured\n# together, the baseline and duplicate detection span both, and the rest of the\n# tree stays out. With `base`, the repo-wide diff scopes all roots.\nanalyze([\"src\", \"ext\"])\n\n# Capture the findings to filter or gate on.\nfindings = analyze(\"src\")\nhigh = filter(f -> f.absolute == :high, active(findings))\n\nanalyze returns Findings, a vector of Findings that prints as a report. To write that report elsewhere, show(io, MIME(\"text/plain\"), findings). A Finding carries the metric, its value, the absolute band (:ok/:warn/:high), and the corpus percentile:\n\nsrc/parser.jl:1070  predicate  cyclomatic 51 (high; p100)\nsrc/parser.jl:1070  predicate  nesting_depth 8 (high; p100)\nsrc/api.jl:289  stub_marker (high)\n\nerrors is the gate companion: only the error-severity findings, so a clean codebase returns nothing and a test suite can assert on it. Gating CI covers it, the ratchet against a base ref, and the pull-request annotations.","title":"Usage"},{"category":"section","location":"#Where-to-read-next","page":"Home","text":"New here, Your first scan runs one and reads the report line by line.\n\nTo get something done:\n\nGating CI is the error floor, the ratchet, and the GitHub Actions setup.\nSuppressing findings is the inline directives and path ignores.\nComparing against a library points a scan at a dependency's source.\nPattern rules writes a project's own rule as a tree-sitter query.\nCustom rules extends or replaces the rule set from Julia.\nAdding a language registers a grammar Dendro does not ship.\nDiagrams draws the graphs behind the findings.\n\nTo look something up:\n\nMetric reference is every metric name, its default band, and whether it runs by default.\nCommand line documents julia -m Dendro, its flags, and threading.\nConfiguration file is the .dendro.toml cascade and every key it takes.\nPattern query syntax is the capture conventions, predicates, and fragments.\nLanguages and limitations lists what is supported and how far resolution reaches.\nAPI reference documents every public function and type.\n\nTo understand what a finding means:\n\nScoring and metrics explains the two-score model and what each metric measures.\nDuplicate detection covers exact clones, near-misses, and reimplementations.\nDuplication against a library asks whether a dependency already does it.\nCohesion and placement reads units referencing units.\nDependencies and layout reads files depending on files.","title":"Where to read next"},{"category":"section","location":"languages-add/#Adding-a-language","page":"Adding a language","text":"The languages Dendro ships queries for are listed under Languages and limitations, along with how far cross-file resolution reaches in each.\n\nAdding a language is a tree-sitter query, a profile entry, and an extension entry. A project supplies all three from its .dendro.toml, so a language Dendro does not ship needs no fork:\n\n[languages.zig]\nextensions = [\"zig\"]\ngrammar = \"/path/to/tree-sitter-zig\"\nqueries = \"/path/to/my-queries\"\n\nqueries is the only required key, naming a directory that holds <name>.scm and optionally <name>.scopes.scm and <name>.imports.scm. grammar defaults to the language name and resolves to tree_sitter_<name>_jll; point it at a directory instead to load a grammar from a local tree-sitter repository, which needs a tree-sitter.json and a built shared library beside it. That is the only route for a grammar with no JLL in the General registry. extensions are the file types the language claims, with or without a leading dot; omit them to keep whatever the built-in table already maps for that name.\n\nA registered language of the same name as a built-in replaces it, which is how a project retunes a shipped query without a fork:\n\n[languages.python]\nqueries = \"my-queries\"\n\nWrite the scopes query rather than leaving it out. Without one the language loses cohesion scoring and every binding-based flag (unused_parameter, unused_local, shadowed_variable, local_count), which is a large share of what Dendro reports. The passes skip a language that ships no scopes query rather than scoring it wrongly.\n\nCross-file resolution needs more than a query: a linkage entry is Julia code in the package, so a registered language reaches the per-file metrics, the flags, and clone detection, but none of the passes built on cross-file references: :misplaced, :scattered, :unreferenced, :split_audience, :back_edge, :dependency_cycle, :hub, :incoherent_package, or :divisible_package.","title":"Adding a language"},{"category":"section","location":"languages-add/#Choices-a-query-has-to-make","page":"Adding a language","text":"The concept vocabulary is closed and a grammar's node types are not, so writing a query means deciding which of a language's constructs answer to each concept. Three of those decisions recur, stated here against Zig, where each one came up.\n\nNot every error construct is a decision point. Zig's try forwards an error rather than choosing between paths, and idiomatic code writes it on nearly every fallible call, so counting it would swamp the metric. catch and orelse do count, since each names an alternative value or block. The shipped Rust query makes the same call for ?.\n\nCompile-time builtins are not calls. Zig's @import and @intCast do not call into the program, and @import alone would dominate every file's callee vocabulary, which is what the reimplementation pass reads.\n\nA scopes query binds a declaration, not an assignment. Zig's grammar leaves the const or var keyword optional, so a bare total = 9; parses as a variable_declaration too, and matching every one of those reads a rebinding as a fresh definition and stops the assigned name from counting as a use. Julia's scopes query makes the same split for the same reason.","title":"Choices a query has to make"},{"category":"section","location":"libraries/#Duplication-against-a-library","page":"Duplication against a library","text":"Every other reading Dendro makes is one corpus judged against itself: is this function long, is this shape written twice here, does this unit belong in that file. This one is asked against code the project does not own.\n\nHas the author written something a library they already ship with does for them?\n\nA generated diff adds a 40-line chunk_by helper; the project already depends on a package exporting exactly that; nothing complains. Point Dendro at the dependency's source and it complains at the site in the project, naming the library symbol to import. Comparing against a library is how to point it.\n\nA library is a reference corpus: source Dendro reads and never scores. It stays out of the baseline, out of the symbol resolution of your code, out of both graphs, out of the per-file rules, and out of the corpus a diff scope is built over. So a dependency ten times the size of your project neither moves your percentile scores nor fills the report with findings about code nobody on your team can edit.\n\nTwo metrics, mirroring the within-corpus pair:\n\n:library_duplicate, an exact structural match against a library anchor.\n:library_near_duplicate, an approximate match against a library function.\n\nThey read differently. An exact match against a public function is \"delete this and import it\"; a 0.87 match is \"the library does nearly this, check the difference\".","title":"Duplication against a library"},{"category":"section","location":"libraries/#What-a-finding-says","page":"Duplication against a library","text":"One location, always in your code, with every library fact in the label:\n\nsrc/util.jl:42  chunk_by  [IterTools.partition public, src/IterTools.jl:88]  library_duplicate 100 (high)\nsrc/parse.jl:210  read_header  [HTTP.parse_line internal, src/parse.jl:44]  library_duplicate 34 (warn)\nsrc/text.jl:12  squeeze  [Base.replace public, strings/util.jl:640; +2 more]  library_near_duplicate 91 (warn)\n\nThe label names the library-qualified symbol, whether it is part of that library's public API, and where it sits relative to the library's own root. Up to three references are named, best first, with a count for the rest.\n\nEvidence lives in the label rather than in a second location, and two things break if it does not. A Location pointing outside the scanned corpus would throw under analyze(; base), the diff-scoped view. And the gate's ratchet keys a finding by its locations, so a library path, version slug and all, would rewrite that key on every dependency upgrade. Neither happens.\n\nThe value is coverage: how much of your enclosing function the matched region is, as a percent. Your whole 30-node function matching a library's 200-node function scores 100, the strongest reading, because all of what you wrote is already there. A 30-node block inside your 200-node function scores 15, a real finding ranked below it. One number, monotone in how much code the edit deletes.","title":"What a finding says"},{"category":"section","location":"libraries/#What-gates-and-what-only-warns","page":"Duplication against a library","text":"the library side coverage band\npublic whole function at or above library_gate_coverage :high\npublic whole function below it :warn\nprivate whole function any :warn\na block inside a function any :warn\n\nThat table is the exact pass. :library_near_duplicate always reports :warn, so it never reaches errors; see \"How the defaults were set\" below for why.\n\nOnly the first row reaches the gate and the CI ratchet, and the two facts in it are only actionable together. A match against a public whole function is importable: there is a name to call instead. A private one is not, though it still says the library solved this and you solved it again, which usually means the abstraction belongs somewhere neither corpus has it yet. And you cannot import half a function, however public the function containing it, so a block match names the containing symbol and leaves the decision to you.\n\nlibrary_gate_coverage defaults to 50. Below half, \"import this instead\" is an edit inside a function you keep rather than a deletion, which is a suggestion and not a violation.\n\nPublicness is read per language from declared exports or a visibility modifier, the same reading :unreferenced uses, with one default inverted. A library in a language Dendro has no linkage model for reads as private, so every match from it warns and nothing gates. :unreferenced reads the same case as public, because there the safe direction is not calling an unverifiable definition dead; here public is what promotes a finding into the gate, so the safe direction is the other one.\n\nA match is attributed to the top-level definition enclosing it, and that is where the publicness reading has a real ceiling. In a language whose methods live inside an impl or class body, most functions are not top-level definitions, so a match inside one attributes to nothing and reads as private. Measured on four Rust crates against their registry sources, every finding came back :warn for exactly this reason: globset declares 45 top-level definitions against 147 function units. The gate is silent there rather than wrong, which is the direction to fail in, but do not read a clean errors run on such a project as evidence that the exact pass found nothing.","title":"What gates and what only warns"},{"category":"section","location":"libraries/#Partial-duplication","page":"Duplication against a library","text":"The library side indexes every anchor, whole functions and the blocks inside them, at the same size floors exact clone detection already sets. Your side is walked the same way, so partial duplication falls out with no extra mechanism: a block of yours matches a block of theirs when twenty lines of your sixty-line function are what their helper does. A maximality filter keeps the largest redundant region, so a duplicated whole function is not also reported for each block inside it.\n\nThe floors carry over unchanged. A function with control flow clears min_size; a control-free function and a block clear twice that. Genuinely useful small helpers (isblank(c) = c == ' ' || c == '\\t') sit below the floor and will not be found. That is the same bargain the clone passes make: a short control-free shape coincides across unrelated code, and reporting it is noise.\n\nThe near pass compares whole functions on both sides by default, so a library function that appears inside a much larger function of yours, edited, is not proposed: the two land in size bands too far apart for the candidate query to pair them. library_anchor_grain widens it to compare every anchor, which finds that case. It costs three to four times the candidate pairs and three and a half to five times the confirmation work, measured across five projects, for four to twenty percent more findings in a pass that never gates, so it is off by default.","title":"Partial duplication"},{"category":"section","location":"libraries/#How-the-defaults-were-set","page":"Duplication against a library","text":"None of these numbers is a guess. Ten Julia projects were scanned against their declared direct dependencies at min_size 10: 7,208 project units against 559 to 6,067 library units each.\n\nThe exact pass produced 74 findings, 3 of them :high. Hand classification of those three: one true positive (Documenter._escapeuri against URIs.escapeuri, a genuine vendoring), and two false positives, both short-form one-liners whose type annotations pushed them over the control-free size floor. Zero to two gate findings per project is a satisfiable gate, and the false positives take one suppression line each.\n\nThe near pass is where the interesting result is. At the originally proposed cutoff it produced 618 findings, 84 of which would have gated: eight gate errors in a healthy project, which is not a gate. Hand classification at its best cutoff put precision on those findings at 11%, against the exact pass's 33%. Raising the cutoff does not help: it thins the population and discards the true positives before the coincidences. So the near pass is capped at :warn and never gates.\n\nThe mechanism behind that gap is worth knowing, because it is a consequence of the design rather than a bug. The cross-corpus match test is max(|LCS|/|a|, |LCS|/|r|), which is |LCS| measured against the shorter side, where the within-corpus near pass measures against the longer. That is deliberate, since a library function almost wholly contained in one of yours is the finding as much as the other way round, but it is a far weaker test, and it runs against a pool of thousands of library units rather than hundreds of your own. Weaker test, more candidates, more coincidences.\n\nlibrary_threshold moved from the proposed 0.85 to 0.90 on the same evidence. There is no measured gap to sit in: the population decays smoothly, 618 findings at 0.85, 207 at 0.90, 51 at 0.95, none at 0.98. 0.90 is a stated trade, the last cutoff that keeps the measured true positives while cutting the noise by two thirds.\n\nlibrary_gate_coverage stayed at 50. It demotes five exact findings that would otherwise gate, four of them false positives, which lifts precision on the gated population from about 20% to 33%.\n\nFour Rust crates were scanned against their registry sources as a check on the other publicness path, a pub modifier rather than an export list. Nothing gated, for the attribution reason above rather than anything about the predicate. What the Rust run did confirm is the interface-boilerplate class: ripgrep's printer reports six exact matches at full coverage against termcolor, every one a trait method (set_color, reset, flush) implemented the way the trait says to.","title":"How the defaults were set"},{"category":"section","location":"libraries/#What-it-does-not-do","page":"Duplication against a library","text":"The passes read subtree hashes and tree shape, never types and never dispatch, the same bargain the rest of Dendro makes. A helper rewritten with a different shape shares no subtrees with the library function it duplicates and neither pass sees it; that is what :reimplementation reads within one corpus, and it does not port across corpora as-is.\n\nA rewrite that also renames the domain vocabulary is invisible to everything here, as it is to :reimplementation.\n\nAnd the gate rests on a publicness reading that is narrower than importability. Julia reads the export/public lists, so a function reachable as Foo.bar but never exported counts as internal and lands at :warn. The pass under-promotes on Julia rather than over-promoting, which is the right direction for something a build fails on.","title":"What it does not do"}]
}
